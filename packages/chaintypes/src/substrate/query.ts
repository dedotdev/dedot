// Generated by @delightfuldot/codegen

import type { GenericChainStorage, Callback, Unsub } from '@delightfuldot/types';
import type {
  AccountId32Like,
  H256,
  Bytes,
  Digest,
  FixedBytes,
  AccountId32,
  FixedU128,
  Perbill,
  Percent,
  Data,
  FixedU64,
} from '@delightfuldot/codecs';
import type {
  FrameSystemAccountInfo,
  FrameSupportDispatchPerDispatchClass,
  FrameSystemEventRecord,
  FrameSystemLastRuntimeUpgradeInfo,
  FrameSystemPhase,
  SpConsensusBabeAppPublic,
  SpConsensusSlotsSlot,
  SpConsensusBabeDigestsNextConfigDescriptor,
  SpConsensusBabeDigestsPreDigest,
  SpConsensusBabeBabeEpochConfiguration,
  PalletBalancesAccountData,
  PalletBalancesBalanceLock,
  PalletBalancesReserveData,
  PalletBalancesIdAmount,
  PalletBalancesIdAmountRuntimeFreezeReason,
  PalletTransactionPaymentReleases,
  PalletElectionProviderMultiPhasePhase,
  PalletElectionProviderMultiPhaseReadySolution,
  PalletElectionProviderMultiPhaseRoundSnapshot,
  PalletElectionProviderMultiPhaseSolutionOrSnapshotSize,
  SpNposElectionsElectionScore,
  PalletElectionProviderMultiPhaseSignedSignedSubmission,
  PalletStakingStakingLedger,
  PalletStakingRewardDestination,
  PalletStakingValidatorPrefs,
  PalletStakingNominations,
  PalletStakingActiveEraInfo,
  SpStakingExposure,
  SpStakingPagedExposureMetadata,
  SpStakingExposurePage,
  PalletStakingEraRewardPoints,
  PalletStakingForcing,
  PalletStakingUnappliedSlash,
  PalletStakingSlashingSlashingSpans,
  PalletStakingSlashingSpanRecord,
  KitchensinkRuntimeSessionKeys,
  SpCoreCryptoKeyTypeId,
  FrameSupportPreimagesBounded,
  PalletDemocracyReferendumInfo,
  PalletDemocracyVoteVoting,
  PalletDemocracyVoteThreshold,
  PalletDemocracyMetadataOwner,
  KitchensinkRuntimeRuntimeCall,
  PalletCollectiveVotes,
  PalletElectionsPhragmenSeatHolder,
  PalletElectionsPhragmenVoter,
  PalletGrandpaStoredState,
  PalletGrandpaStoredPendingChange,
  SpConsensusGrandpaAppPublic,
  PalletTreasuryProposal,
  PalletTreasurySpendStatus,
  PalletContractsWasmCodeInfo,
  PalletContractsStorageContractInfo,
  PalletContractsStorageDeletionQueueManager,
  PalletImOnlineSr25519AppSr25519Public,
  SpAuthorityDiscoveryAppPublic,
  SpStakingOffenceOffenceDetails,
  PalletIdentityRegistration,
  PalletIdentityRegistrarInfo,
  PalletSocietyGroupParams,
  PalletSocietyMemberRecord,
  PalletSocietyPayoutRecord,
  PalletSocietyBid,
  PalletSocietyCandidacy,
  PalletSocietyVote,
  PalletSocietyIntakeRecord,
  PalletSocietyTally,
  PalletRecoveryRecoveryConfig,
  PalletRecoveryActiveRecovery,
  PalletVestingVestingInfo,
  PalletVestingReleases,
  PalletSchedulerScheduled,
  PalletPreimageOldRequestStatus,
  PalletPreimageRequestStatus,
  PalletProxyProxyDefinition,
  PalletProxyAnnouncement,
  PalletMultisigMultisig,
  PalletBountiesBounty,
  PalletTipsOpenTip,
  PalletAssetsAssetDetails,
  PalletAssetsAssetAccount,
  PalletAssetsApproval,
  PalletAssetsAssetMetadata,
  PalletLotteryLotteryConfig,
  PalletNisBid,
  PalletNisSummaryRecord,
  PalletNisReceiptRecord,
  PalletUniquesCollectionDetails,
  PalletUniquesItemDetails,
  PalletUniquesCollectionMetadata,
  PalletUniquesItemMetadata,
  PalletNftsCollectionDetails,
  PalletNftsBitFlagsCollectionRole,
  PalletNftsItemDetails,
  PalletNftsCollectionMetadata,
  PalletNftsItemMetadata,
  PalletNftsAttributeDeposit,
  PalletNftsAttributeNamespace,
  PalletNftsPendingSwap,
  PalletNftsCollectionConfig,
  PalletNftsItemConfig,
  PalletNftFractionalizationDetails,
  PalletSalaryStatusType,
  PalletSalaryClaimantStatus,
  PalletCoreFellowshipParamsType,
  PalletCoreFellowshipMemberStatus,
  PalletCoreFellowshipWish,
  PalletTransactionStorageTransactionInfo,
  PalletBagsListListNode,
  PalletBagsListListBag,
  PalletStateTrieMigrationMigrationTask,
  PalletStateTrieMigrationMigrationLimits,
  PalletChildBountiesChildBounty,
  PalletReferendaReferendumInfo,
  PalletConvictionVotingVoteVoting,
  PalletAllianceCid,
  PalletAllianceMemberRole,
  PalletNominationPoolsPoolMember,
  PalletNominationPoolsBondedPoolInner,
  PalletNominationPoolsRewardPool,
  PalletNominationPoolsSubPools,
  PalletNominationPoolsClaimPermission,
  PalletReferendaReferendumInfoTally,
  PalletRankedCollectiveMemberRecord,
  PalletRankedCollectiveVoteRecord,
  PalletAssetConversionPoolInfo,
  PalletAssetConversionNativeOrAssetId,
  PalletFastUnstakeUnstakeRequest,
  PalletMessageQueueBookState,
  PalletMessageQueuePage,
  PalletBrokerConfigRecord,
  PalletBrokerScheduleItem,
  PalletBrokerLeaseRecordItem,
  PalletBrokerStatusRecord,
  PalletBrokerSaleInfoRecord,
  PalletBrokerAllowedRenewalRecord,
  PalletBrokerAllowedRenewalId,
  PalletBrokerRegionRecord,
  PalletBrokerRegionId,
  PalletBrokerContributionRecord,
  PalletBrokerPoolIoRecord,
  PalletBrokerInstaPoolHistoryRecord,
  SpMixnetAppPublic,
  PalletMixnetBoundedMixnode,
} from './types';

export interface ChainStorage extends GenericChainStorage {
  system: {
    /**
     * The full account information for a particular account ID.
     **/
    account(arg: AccountId32Like): Promise<FrameSystemAccountInfo>;
    account(arg: AccountId32Like, callback: Callback<FrameSystemAccountInfo>): Promise<Unsub>;

    /**
     * Total extrinsics count for the current block.
     **/
    extrinsicCount(): Promise<number | undefined>;
    extrinsicCount(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The current weight for the block.
     **/
    blockWeight(): Promise<FrameSupportDispatchPerDispatchClass>;
    blockWeight(callback: Callback<FrameSupportDispatchPerDispatchClass>): Promise<Unsub>;

    /**
     * Total length (in bytes) for all extrinsics put together, for the current block.
     **/
    allExtrinsicsLen(): Promise<number | undefined>;
    allExtrinsicsLen(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Map of block numbers to block hashes.
     **/
    blockHash(arg: number): Promise<H256>;
    blockHash(arg: number, callback: Callback<H256>): Promise<Unsub>;

    /**
     * Extrinsics data for the current block (maps an extrinsic's index to its data).
     **/
    extrinsicData(arg: number): Promise<Bytes>;
    extrinsicData(arg: number, callback: Callback<Bytes>): Promise<Unsub>;

    /**
     * The current block number being processed. Set by `execute_block`.
     **/
    number(): Promise<number>;
    number(callback: Callback<number>): Promise<Unsub>;

    /**
     * Hash of the previous block.
     **/
    parentHash(): Promise<H256>;
    parentHash(callback: Callback<H256>): Promise<Unsub>;

    /**
     * Digest of the current block, also part of the block header.
     **/
    digest(): Promise<Digest>;
    digest(callback: Callback<Digest>): Promise<Unsub>;

    /**
     * Events deposited for the current block.
     *
     * NOTE: The item is unbound and should therefore never be read on chain.
     * It could otherwise inflate the PoV size of a block.
     *
     * Events have a large in-memory size. Box the events to not go out-of-memory
     * just in case someone still reads them from within the runtime.
     **/
    events(): Promise<Array<FrameSystemEventRecord>>;
    events(callback: Callback<Array<FrameSystemEventRecord>>): Promise<Unsub>;

    /**
     * The number of events in the `Events<T>` list.
     **/
    eventCount(): Promise<number>;
    eventCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Mapping between a topic (represented by T::Hash) and a vector of indexes
     * of events in the `<Events<T>>` list.
     *
     * All topic vectors have deterministic storage locations depending on the topic. This
     * allows light-clients to leverage the changes trie storage tracking mechanism and
     * in case of changes fetch the list of events of interest.
     *
     * The value has the type `(BlockNumberFor<T>, EventIndex)` because if we used only just
     * the `EventIndex` then in case if the topic has the same contents on the next block
     * no notification will be triggered thus the event might be lost.
     **/
    eventTopics(arg: H256): Promise<Array<[number, number]>>;
    eventTopics(arg: H256, callback: Callback<Array<[number, number]>>): Promise<Unsub>;

    /**
     * Stores the `spec_version` and `spec_name` of when the last runtime upgrade happened.
     **/
    lastRuntimeUpgrade(): Promise<FrameSystemLastRuntimeUpgradeInfo | undefined>;
    lastRuntimeUpgrade(callback: Callback<FrameSystemLastRuntimeUpgradeInfo | undefined>): Promise<Unsub>;

    /**
     * True if we have upgraded so that `type RefCount` is `u32`. False (default) if not.
     **/
    upgradedToU32RefCount(): Promise<boolean>;
    upgradedToU32RefCount(callback: Callback<boolean>): Promise<Unsub>;

    /**
     * True if we have upgraded so that AccountInfo contains three types of `RefCount`. False
     * (default) if not.
     **/
    upgradedToTripleRefCount(): Promise<boolean>;
    upgradedToTripleRefCount(callback: Callback<boolean>): Promise<Unsub>;

    /**
     * The execution phase of the block.
     **/
    executionPhase(): Promise<FrameSystemPhase | undefined>;
    executionPhase(callback: Callback<FrameSystemPhase | undefined>): Promise<Unsub>;
  };
  babe: {
    /**
     * Current epoch index.
     **/
    epochIndex(): Promise<bigint>;
    epochIndex(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Current epoch authorities.
     **/
    authorities(): Promise<Array<[SpConsensusBabeAppPublic, bigint]>>;
    authorities(callback: Callback<Array<[SpConsensusBabeAppPublic, bigint]>>): Promise<Unsub>;

    /**
     * The slot at which the first epoch actually started. This is 0
     * until the first block of the chain.
     **/
    genesisSlot(): Promise<SpConsensusSlotsSlot>;
    genesisSlot(callback: Callback<SpConsensusSlotsSlot>): Promise<Unsub>;

    /**
     * Current slot number.
     **/
    currentSlot(): Promise<SpConsensusSlotsSlot>;
    currentSlot(callback: Callback<SpConsensusSlotsSlot>): Promise<Unsub>;

    /**
     * The epoch randomness for the *current* epoch.
     *
     * # Security
     *
     * This MUST NOT be used for gambling, as it can be influenced by a
     * malicious validator in the short term. It MAY be used in many
     * cryptographic protocols, however, so long as one remembers that this
     * (like everything else on-chain) it is public. For example, it can be
     * used where a number is needed that cannot have been chosen by an
     * adversary, for purposes such as public-coin zero-knowledge proofs.
     **/
    randomness(): Promise<FixedBytes<32>>;
    randomness(callback: Callback<FixedBytes<32>>): Promise<Unsub>;

    /**
     * Pending epoch configuration change that will be applied when the next epoch is enacted.
     **/
    pendingEpochConfigChange(): Promise<SpConsensusBabeDigestsNextConfigDescriptor | undefined>;
    pendingEpochConfigChange(
      callback: Callback<SpConsensusBabeDigestsNextConfigDescriptor | undefined>,
    ): Promise<Unsub>;

    /**
     * Next epoch randomness.
     **/
    nextRandomness(): Promise<FixedBytes<32>>;
    nextRandomness(callback: Callback<FixedBytes<32>>): Promise<Unsub>;

    /**
     * Next epoch authorities.
     **/
    nextAuthorities(): Promise<Array<[SpConsensusBabeAppPublic, bigint]>>;
    nextAuthorities(callback: Callback<Array<[SpConsensusBabeAppPublic, bigint]>>): Promise<Unsub>;

    /**
     * Randomness under construction.
     *
     * We make a trade-off between storage accesses and list length.
     * We store the under-construction randomness in segments of up to
     * `UNDER_CONSTRUCTION_SEGMENT_LENGTH`.
     *
     * Once a segment reaches this length, we begin the next one.
     * We reset all segments and return to `0` at the beginning of every
     * epoch.
     **/
    segmentIndex(): Promise<number>;
    segmentIndex(callback: Callback<number>): Promise<Unsub>;

    /**
     * TWOX-NOTE: `SegmentIndex` is an increasing integer, so this is okay.
     **/
    underConstruction(arg: number): Promise<Array<FixedBytes<32>>>;
    underConstruction(arg: number, callback: Callback<Array<FixedBytes<32>>>): Promise<Unsub>;

    /**
     * Temporary value (cleared at block finalization) which is `Some`
     * if per-block initialization has already been called for current block.
     **/
    initialized(): Promise<SpConsensusBabeDigestsPreDigest | undefined | undefined>;
    initialized(callback: Callback<SpConsensusBabeDigestsPreDigest | undefined | undefined>): Promise<Unsub>;

    /**
     * This field should always be populated during block processing unless
     * secondary plain slots are enabled (which don't contain a VRF output).
     *
     * It is set in `on_finalize`, before it will contain the value from the last block.
     **/
    authorVrfRandomness(): Promise<FixedBytes<32> | undefined>;
    authorVrfRandomness(callback: Callback<FixedBytes<32> | undefined>): Promise<Unsub>;

    /**
     * The block numbers when the last and current epoch have started, respectively `N-1` and
     * `N`.
     * NOTE: We track this is in order to annotate the block number when a given pool of
     * entropy was fixed (i.e. it was known to chain observers). Since epochs are defined in
     * slots, which may be skipped, the block numbers may not line up with the slot numbers.
     **/
    epochStart(): Promise<[number, number]>;
    epochStart(callback: Callback<[number, number]>): Promise<Unsub>;

    /**
     * How late the current block is compared to its parent.
     *
     * This entry is populated as part of block execution and is cleaned up
     * on block finalization. Querying this storage entry outside of block
     * execution context should always yield zero.
     **/
    lateness(): Promise<number>;
    lateness(callback: Callback<number>): Promise<Unsub>;

    /**
     * The configuration for the current epoch. Should never be `None` as it is initialized in
     * genesis.
     **/
    epochConfig(): Promise<SpConsensusBabeBabeEpochConfiguration | undefined>;
    epochConfig(callback: Callback<SpConsensusBabeBabeEpochConfiguration | undefined>): Promise<Unsub>;

    /**
     * The configuration for the next epoch, `None` if the config will not change
     * (you can fallback to `EpochConfig` instead in that case).
     **/
    nextEpochConfig(): Promise<SpConsensusBabeBabeEpochConfiguration | undefined>;
    nextEpochConfig(callback: Callback<SpConsensusBabeBabeEpochConfiguration | undefined>): Promise<Unsub>;

    /**
     * A list of the last 100 skipped epochs and the corresponding session index
     * when the epoch was skipped.
     *
     * This is only used for validating equivocation proofs. An equivocation proof
     * must contains a key-ownership proof for a given session, therefore we need a
     * way to tie together sessions and epoch indices, i.e. we need to validate that
     * a validator was the owner of a given key on a given session, and what the
     * active epoch index was during that session.
     **/
    skippedEpochs(): Promise<Array<[bigint, number]>>;
    skippedEpochs(callback: Callback<Array<[bigint, number]>>): Promise<Unsub>;
  };
  timestamp: {
    /**
     * The current time for the current block.
     **/
    now(): Promise<bigint>;
    now(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Whether the timestamp has been updated in this block.
     *
     * This value is updated to `true` upon successful submission of a timestamp by a node.
     * It is then checked at the end of each block execution in the `on_finalize` hook.
     **/
    didUpdate(): Promise<boolean>;
    didUpdate(callback: Callback<boolean>): Promise<Unsub>;
  };
  authorship: {
    /**
     * Author of current block.
     **/
    author(): Promise<AccountId32 | undefined>;
    author(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  indices: {
    /**
     * The lookup from index to account.
     **/
    accounts(arg: number): Promise<[AccountId32, bigint, boolean] | undefined>;
    accounts(arg: number, callback: Callback<[AccountId32, bigint, boolean] | undefined>): Promise<Unsub>;
  };
  balances: {
    /**
     * The total units issued in the system.
     **/
    totalIssuance(): Promise<bigint>;
    totalIssuance(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * The total units of outstanding deactivated balance in the system.
     **/
    inactiveIssuance(): Promise<bigint>;
    inactiveIssuance(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * The Balances pallet example of storing the balance of an account.
     *
     * # Example
     *
     * ```nocompile
     * impl pallet_balances::Config for Runtime {
     * type AccountStore = StorageMapShim<Self::Account<Runtime>, frame_system::Provider<Runtime>, AccountId, Self::AccountData<Balance>>
     * }
     * ```
     *
     * You can also store the balance of an account in the `System` pallet.
     *
     * # Example
     *
     * ```nocompile
     * impl pallet_balances::Config for Runtime {
     * type AccountStore = System
     * }
     * ```
     *
     * But this comes with tradeoffs, storing account balances in the system pallet stores
     * `frame_system` data alongside the account data contrary to storing account balances in the
     * `Balances` pallet, which uses a `StorageMap` to store balances data only.
     * NOTE: This is only used in the case that this pallet is used to store balances.
     **/
    account(arg: AccountId32Like): Promise<PalletBalancesAccountData>;
    account(arg: AccountId32Like, callback: Callback<PalletBalancesAccountData>): Promise<Unsub>;

    /**
     * Any liquidity locks on some account balances.
     * NOTE: Should only be accessed when setting, changing and freeing a lock.
     **/
    locks(arg: AccountId32Like): Promise<Array<PalletBalancesBalanceLock>>;
    locks(arg: AccountId32Like, callback: Callback<Array<PalletBalancesBalanceLock>>): Promise<Unsub>;

    /**
     * Named reserves on some account balances.
     **/
    reserves(arg: AccountId32Like): Promise<Array<PalletBalancesReserveData>>;
    reserves(arg: AccountId32Like, callback: Callback<Array<PalletBalancesReserveData>>): Promise<Unsub>;

    /**
     * Holds on account balances.
     **/
    holds(arg: AccountId32Like): Promise<Array<PalletBalancesIdAmount>>;
    holds(arg: AccountId32Like, callback: Callback<Array<PalletBalancesIdAmount>>): Promise<Unsub>;

    /**
     * Freeze locks on account balances.
     **/
    freezes(arg: AccountId32Like): Promise<Array<PalletBalancesIdAmountRuntimeFreezeReason>>;
    freezes(arg: AccountId32Like, callback: Callback<Array<PalletBalancesIdAmountRuntimeFreezeReason>>): Promise<Unsub>;
  };
  transactionPayment: {
    nextFeeMultiplier(): Promise<FixedU128>;
    nextFeeMultiplier(callback: Callback<FixedU128>): Promise<Unsub>;
    storageVersion(): Promise<PalletTransactionPaymentReleases>;
    storageVersion(callback: Callback<PalletTransactionPaymentReleases>): Promise<Unsub>;
  };
  electionProviderMultiPhase: {
    /**
     * Internal counter for the number of rounds.
     *
     * This is useful for de-duplication of transactions submitted to the pool, and general
     * diagnostics of the pallet.
     *
     * This is merely incremented once per every time that an upstream `elect` is called.
     **/
    round(): Promise<number>;
    round(callback: Callback<number>): Promise<Unsub>;

    /**
     * Current phase.
     **/
    currentPhase(): Promise<PalletElectionProviderMultiPhasePhase>;
    currentPhase(callback: Callback<PalletElectionProviderMultiPhasePhase>): Promise<Unsub>;

    /**
     * Current best solution, signed or unsigned, queued to be returned upon `elect`.
     *
     * Always sorted by score.
     **/
    queuedSolution(): Promise<PalletElectionProviderMultiPhaseReadySolution | undefined>;
    queuedSolution(callback: Callback<PalletElectionProviderMultiPhaseReadySolution | undefined>): Promise<Unsub>;

    /**
     * Snapshot data of the round.
     *
     * This is created at the beginning of the signed phase and cleared upon calling `elect`.
     **/
    snapshot(): Promise<PalletElectionProviderMultiPhaseRoundSnapshot | undefined>;
    snapshot(callback: Callback<PalletElectionProviderMultiPhaseRoundSnapshot | undefined>): Promise<Unsub>;

    /**
     * Desired number of targets to elect for this round.
     *
     * Only exists when [`Snapshot`] is present.
     **/
    desiredTargets(): Promise<number | undefined>;
    desiredTargets(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The metadata of the [`RoundSnapshot`]
     *
     * Only exists when [`Snapshot`] is present.
     **/
    snapshotMetadata(): Promise<PalletElectionProviderMultiPhaseSolutionOrSnapshotSize | undefined>;
    snapshotMetadata(
      callback: Callback<PalletElectionProviderMultiPhaseSolutionOrSnapshotSize | undefined>,
    ): Promise<Unsub>;

    /**
     * The next index to be assigned to an incoming signed submission.
     *
     * Every accepted submission is assigned a unique index; that index is bound to that particular
     * submission for the duration of the election. On election finalization, the next index is
     * reset to 0.
     *
     * We can't just use `SignedSubmissionIndices.len()`, because that's a bounded set; past its
     * capacity, it will simply saturate. We can't just iterate over `SignedSubmissionsMap`,
     * because iteration is slow. Instead, we store the value here.
     **/
    signedSubmissionNextIndex(): Promise<number>;
    signedSubmissionNextIndex(callback: Callback<number>): Promise<Unsub>;

    /**
     * A sorted, bounded vector of `(score, block_number, index)`, where each `index` points to a
     * value in `SignedSubmissions`.
     *
     * We never need to process more than a single signed submission at a time. Signed submissions
     * can be quite large, so we're willing to pay the cost of multiple database accesses to access
     * them one at a time instead of reading and decoding all of them at once.
     **/
    signedSubmissionIndices(): Promise<Array<[SpNposElectionsElectionScore, number, number]>>;
    signedSubmissionIndices(callback: Callback<Array<[SpNposElectionsElectionScore, number, number]>>): Promise<Unsub>;

    /**
     * Unchecked, signed solutions.
     *
     * Together with `SubmissionIndices`, this stores a bounded set of `SignedSubmissions` while
     * allowing us to keep only a single one in memory at a time.
     *
     * Twox note: the key of the map is an auto-incrementing index which users cannot inspect or
     * affect; we shouldn't need a cryptographically secure hasher.
     **/
    signedSubmissionsMap(arg: number): Promise<PalletElectionProviderMultiPhaseSignedSignedSubmission | undefined>;
    signedSubmissionsMap(
      arg: number,
      callback: Callback<PalletElectionProviderMultiPhaseSignedSignedSubmission | undefined>,
    ): Promise<Unsub>;

    /**
     * The minimum score that each 'untrusted' solution must attain in order to be considered
     * feasible.
     *
     * Can be set via `set_minimum_untrusted_score`.
     **/
    minimumUntrustedScore(): Promise<SpNposElectionsElectionScore | undefined>;
    minimumUntrustedScore(callback: Callback<SpNposElectionsElectionScore | undefined>): Promise<Unsub>;
  };
  staking: {
    /**
     * The ideal number of active validators.
     **/
    validatorCount(): Promise<number>;
    validatorCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Minimum number of staking participants before emergency conditions are imposed.
     **/
    minimumValidatorCount(): Promise<number>;
    minimumValidatorCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Any validators that may never be slashed or forcibly kicked. It's a Vec since they're
     * easy to initialize and the performance hit is minimal (we expect no more than four
     * invulnerables) and restricted to testnets.
     **/
    invulnerables(): Promise<Array<AccountId32>>;
    invulnerables(callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * Map from all locked "stash" accounts to the controller account.
     *
     * TWOX-NOTE: SAFE since `AccountId` is a secure hash.
     **/
    bonded(arg: AccountId32Like): Promise<AccountId32 | undefined>;
    bonded(arg: AccountId32Like, callback: Callback<AccountId32 | undefined>): Promise<Unsub>;

    /**
     * The minimum active bond to become and maintain the role of a nominator.
     **/
    minNominatorBond(): Promise<bigint>;
    minNominatorBond(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * The minimum active bond to become and maintain the role of a validator.
     **/
    minValidatorBond(): Promise<bigint>;
    minValidatorBond(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * The minimum active nominator stake of the last successful election.
     **/
    minimumActiveStake(): Promise<bigint>;
    minimumActiveStake(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * The minimum amount of commission that validators can set.
     *
     * If set to `0`, no limit exists.
     **/
    minCommission(): Promise<Perbill>;
    minCommission(callback: Callback<Perbill>): Promise<Unsub>;

    /**
     * Map from all (unlocked) "controller" accounts to the info regarding the staking.
     *
     * Note: All the reads and mutations to this storage *MUST* be done through the methods exposed
     * by [`StakingLedger`] to ensure data and lock consistency.
     **/
    ledger(arg: AccountId32Like): Promise<PalletStakingStakingLedger | undefined>;
    ledger(arg: AccountId32Like, callback: Callback<PalletStakingStakingLedger | undefined>): Promise<Unsub>;

    /**
     * Where the reward payment should be made. Keyed by stash.
     *
     * TWOX-NOTE: SAFE since `AccountId` is a secure hash.
     **/
    payee(arg: AccountId32Like): Promise<PalletStakingRewardDestination>;
    payee(arg: AccountId32Like, callback: Callback<PalletStakingRewardDestination>): Promise<Unsub>;

    /**
     * The map from (wannabe) validator stash key to the preferences of that validator.
     *
     * TWOX-NOTE: SAFE since `AccountId` is a secure hash.
     **/
    validators(arg: AccountId32Like): Promise<PalletStakingValidatorPrefs>;
    validators(arg: AccountId32Like, callback: Callback<PalletStakingValidatorPrefs>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForValidators(): Promise<number>;
    counterForValidators(callback: Callback<number>): Promise<Unsub>;

    /**
     * The maximum validator count before we stop allowing new validators to join.
     *
     * When this value is not set, no limits are enforced.
     **/
    maxValidatorsCount(): Promise<number | undefined>;
    maxValidatorsCount(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The map from nominator stash key to their nomination preferences, namely the validators that
     * they wish to support.
     *
     * Note that the keys of this storage map might become non-decodable in case the
     * account's [`NominationsQuota::MaxNominations`] configuration is decreased.
     * In this rare case, these nominators
     * are still existent in storage, their key is correct and retrievable (i.e. `contains_key`
     * indicates that they exist), but their value cannot be decoded. Therefore, the non-decodable
     * nominators will effectively not-exist, until they re-submit their preferences such that it
     * is within the bounds of the newly set `Config::MaxNominations`.
     *
     * This implies that `::iter_keys().count()` and `::iter().count()` might return different
     * values for this map. Moreover, the main `::count()` is aligned with the former, namely the
     * number of keys that exist.
     *
     * Lastly, if any of the nominators become non-decodable, they can be chilled immediately via
     * [`Call::chill_other`] dispatchable by anyone.
     *
     * TWOX-NOTE: SAFE since `AccountId` is a secure hash.
     **/
    nominators(arg: AccountId32Like): Promise<PalletStakingNominations | undefined>;
    nominators(arg: AccountId32Like, callback: Callback<PalletStakingNominations | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForNominators(): Promise<number>;
    counterForNominators(callback: Callback<number>): Promise<Unsub>;

    /**
     * The maximum nominator count before we stop allowing new validators to join.
     *
     * When this value is not set, no limits are enforced.
     **/
    maxNominatorsCount(): Promise<number | undefined>;
    maxNominatorsCount(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The current era index.
     *
     * This is the latest planned era, depending on how the Session pallet queues the validator
     * set, it might be active or not.
     **/
    currentEra(): Promise<number | undefined>;
    currentEra(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The active era information, it holds index and start.
     *
     * The active era is the era being currently rewarded. Validator set of this era must be
     * equal to [`SessionInterface::validators`].
     **/
    activeEra(): Promise<PalletStakingActiveEraInfo | undefined>;
    activeEra(callback: Callback<PalletStakingActiveEraInfo | undefined>): Promise<Unsub>;

    /**
     * The session index at which the era start for the last [`Config::HistoryDepth`] eras.
     *
     * Note: This tracks the starting session (i.e. session index when era start being active)
     * for the eras in `[CurrentEra - HISTORY_DEPTH, CurrentEra]`.
     **/
    erasStartSessionIndex(arg: number): Promise<number | undefined>;
    erasStartSessionIndex(arg: number, callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Exposure of validator at era.
     *
     * This is keyed first by the era index to allow bulk deletion and then the stash account.
     *
     * Is it removed after [`Config::HistoryDepth`] eras.
     * If stakers hasn't been set or has been removed then empty exposure is returned.
     *
     * Note: Deprecated since v14. Use `EraInfo` instead to work with exposures.
     **/
    erasStakers(arg: [number, AccountId32Like]): Promise<SpStakingExposure>;
    erasStakers(arg: [number, AccountId32Like], callback: Callback<SpStakingExposure>): Promise<Unsub>;

    /**
     * Summary of validator exposure at a given era.
     *
     * This contains the total stake in support of the validator and their own stake. In addition,
     * it can also be used to get the number of nominators backing this validator and the number of
     * exposure pages they are divided into. The page count is useful to determine the number of
     * pages of rewards that needs to be claimed.
     *
     * This is keyed first by the era index to allow bulk deletion and then the stash account.
     * Should only be accessed through `EraInfo`.
     *
     * Is it removed after [`Config::HistoryDepth`] eras.
     * If stakers hasn't been set or has been removed then empty overview is returned.
     **/
    erasStakersOverview(arg: [number, AccountId32Like]): Promise<SpStakingPagedExposureMetadata | undefined>;
    erasStakersOverview(
      arg: [number, AccountId32Like],
      callback: Callback<SpStakingPagedExposureMetadata | undefined>,
    ): Promise<Unsub>;

    /**
     * Clipped Exposure of validator at era.
     *
     * Note: This is deprecated, should be used as read-only and will be removed in the future.
     * New `Exposure`s are stored in a paged manner in `ErasStakersPaged` instead.
     *
     * This is similar to [`ErasStakers`] but number of nominators exposed is reduced to the
     * `T::MaxExposurePageSize` biggest stakers.
     * (Note: the field `total` and `own` of the exposure remains unchanged).
     * This is used to limit the i/o cost for the nominator payout.
     *
     * This is keyed fist by the era index to allow bulk deletion and then the stash account.
     *
     * It is removed after [`Config::HistoryDepth`] eras.
     * If stakers hasn't been set or has been removed then empty exposure is returned.
     *
     * Note: Deprecated since v14. Use `EraInfo` instead to work with exposures.
     **/
    erasStakersClipped(arg: [number, AccountId32Like]): Promise<SpStakingExposure>;
    erasStakersClipped(arg: [number, AccountId32Like], callback: Callback<SpStakingExposure>): Promise<Unsub>;

    /**
     * Paginated exposure of a validator at given era.
     *
     * This is keyed first by the era index to allow bulk deletion, then stash account and finally
     * the page. Should only be accessed through `EraInfo`.
     *
     * This is cleared after [`Config::HistoryDepth`] eras.
     **/
    erasStakersPaged(arg: [number, AccountId32Like, number]): Promise<SpStakingExposurePage | undefined>;
    erasStakersPaged(
      arg: [number, AccountId32Like, number],
      callback: Callback<SpStakingExposurePage | undefined>,
    ): Promise<Unsub>;

    /**
     * History of claimed paged rewards by era and validator.
     *
     * This is keyed by era and validator stash which maps to the set of page indexes which have
     * been claimed.
     *
     * It is removed after [`Config::HistoryDepth`] eras.
     **/
    claimedRewards(arg: [number, AccountId32Like]): Promise<Array<number>>;
    claimedRewards(arg: [number, AccountId32Like], callback: Callback<Array<number>>): Promise<Unsub>;

    /**
     * Similar to `ErasStakers`, this holds the preferences of validators.
     *
     * This is keyed first by the era index to allow bulk deletion and then the stash account.
     *
     * Is it removed after [`Config::HistoryDepth`] eras.
     **/
    erasValidatorPrefs(arg: [number, AccountId32Like]): Promise<PalletStakingValidatorPrefs>;
    erasValidatorPrefs(arg: [number, AccountId32Like], callback: Callback<PalletStakingValidatorPrefs>): Promise<Unsub>;

    /**
     * The total validator era payout for the last [`Config::HistoryDepth`] eras.
     *
     * Eras that haven't finished yet or has been removed doesn't have reward.
     **/
    erasValidatorReward(arg: number): Promise<bigint | undefined>;
    erasValidatorReward(arg: number, callback: Callback<bigint | undefined>): Promise<Unsub>;

    /**
     * Rewards for the last [`Config::HistoryDepth`] eras.
     * If reward hasn't been set or has been removed then 0 reward is returned.
     **/
    erasRewardPoints(arg: number): Promise<PalletStakingEraRewardPoints>;
    erasRewardPoints(arg: number, callback: Callback<PalletStakingEraRewardPoints>): Promise<Unsub>;

    /**
     * The total amount staked for the last [`Config::HistoryDepth`] eras.
     * If total hasn't been set or has been removed then 0 stake is returned.
     **/
    erasTotalStake(arg: number): Promise<bigint>;
    erasTotalStake(arg: number, callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Mode of era forcing.
     **/
    forceEra(): Promise<PalletStakingForcing>;
    forceEra(callback: Callback<PalletStakingForcing>): Promise<Unsub>;

    /**
     * The percentage of the slash that is distributed to reporters.
     *
     * The rest of the slashed value is handled by the `Slash`.
     **/
    slashRewardFraction(): Promise<Perbill>;
    slashRewardFraction(callback: Callback<Perbill>): Promise<Unsub>;

    /**
     * The amount of currency given to reporters of a slash event which was
     * canceled by extraordinary circumstances (e.g. governance).
     **/
    canceledSlashPayout(): Promise<bigint>;
    canceledSlashPayout(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * All unapplied slashes that are queued for later.
     **/
    unappliedSlashes(arg: number): Promise<Array<PalletStakingUnappliedSlash>>;
    unappliedSlashes(arg: number, callback: Callback<Array<PalletStakingUnappliedSlash>>): Promise<Unsub>;

    /**
     * A mapping from still-bonded eras to the first session index of that era.
     *
     * Must contains information for eras for the range:
     * `[active_era - bounding_duration; active_era]`
     **/
    bondedEras(): Promise<Array<[number, number]>>;
    bondedEras(callback: Callback<Array<[number, number]>>): Promise<Unsub>;

    /**
     * All slashing events on validators, mapped by era to the highest slash proportion
     * and slash value of the era.
     **/
    validatorSlashInEra(arg: [number, AccountId32Like]): Promise<[Perbill, bigint] | undefined>;
    validatorSlashInEra(
      arg: [number, AccountId32Like],
      callback: Callback<[Perbill, bigint] | undefined>,
    ): Promise<Unsub>;

    /**
     * All slashing events on nominators, mapped by era to the highest slash value of the era.
     **/
    nominatorSlashInEra(arg: [number, AccountId32Like]): Promise<bigint | undefined>;
    nominatorSlashInEra(arg: [number, AccountId32Like], callback: Callback<bigint | undefined>): Promise<Unsub>;

    /**
     * Slashing spans for stash accounts.
     **/
    slashingSpans(arg: AccountId32Like): Promise<PalletStakingSlashingSlashingSpans | undefined>;
    slashingSpans(
      arg: AccountId32Like,
      callback: Callback<PalletStakingSlashingSlashingSpans | undefined>,
    ): Promise<Unsub>;

    /**
     * Records information about the maximum slash of a stash within a slashing span,
     * as well as how much reward has been paid out.
     **/
    spanSlash(arg: [AccountId32Like, number]): Promise<PalletStakingSlashingSpanRecord>;
    spanSlash(arg: [AccountId32Like, number], callback: Callback<PalletStakingSlashingSpanRecord>): Promise<Unsub>;

    /**
     * The last planned session scheduled by the session pallet.
     *
     * This is basically in sync with the call to [`pallet_session::SessionManager::new_session`].
     **/
    currentPlannedSession(): Promise<number>;
    currentPlannedSession(callback: Callback<number>): Promise<Unsub>;

    /**
     * Indices of validators that have offended in the active era and whether they are currently
     * disabled.
     *
     * This value should be a superset of disabled validators since not all offences lead to the
     * validator being disabled (if there was no slash). This is needed to track the percentage of
     * validators that have offended in the current era, ensuring a new era is forced if
     * `OffendingValidatorsThreshold` is reached. The vec is always kept sorted so that we can find
     * whether a given validator has previously offended using binary search. It gets cleared when
     * the era ends.
     **/
    offendingValidators(): Promise<Array<[number, boolean]>>;
    offendingValidators(callback: Callback<Array<[number, boolean]>>): Promise<Unsub>;

    /**
     * The threshold for when users can start calling `chill_other` for other validators /
     * nominators. The threshold is compared to the actual number of validators / nominators
     * (`CountFor*`) in the system compared to the configured max (`Max*Count`).
     **/
    chillThreshold(): Promise<Percent | undefined>;
    chillThreshold(callback: Callback<Percent | undefined>): Promise<Unsub>;
  };
  session: {
    /**
     * The current set of validators.
     **/
    validators(): Promise<Array<AccountId32>>;
    validators(callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * Current index of the session.
     **/
    currentIndex(): Promise<number>;
    currentIndex(callback: Callback<number>): Promise<Unsub>;

    /**
     * True if the underlying economic identities or weighting behind the validators
     * has changed in the queued validator set.
     **/
    queuedChanged(): Promise<boolean>;
    queuedChanged(callback: Callback<boolean>): Promise<Unsub>;

    /**
     * The queued keys for the next session. When the next session begins, these keys
     * will be used to determine the validator's session keys.
     **/
    queuedKeys(): Promise<Array<[AccountId32, KitchensinkRuntimeSessionKeys]>>;
    queuedKeys(callback: Callback<Array<[AccountId32, KitchensinkRuntimeSessionKeys]>>): Promise<Unsub>;

    /**
     * Indices of disabled validators.
     *
     * The vec is always kept sorted so that we can find whether a given validator is
     * disabled using binary search. It gets cleared when `on_session_ending` returns
     * a new set of identities.
     **/
    disabledValidators(): Promise<Array<number>>;
    disabledValidators(callback: Callback<Array<number>>): Promise<Unsub>;

    /**
     * The next session keys for a validator.
     **/
    nextKeys(arg: AccountId32Like): Promise<KitchensinkRuntimeSessionKeys | undefined>;
    nextKeys(arg: AccountId32Like, callback: Callback<KitchensinkRuntimeSessionKeys | undefined>): Promise<Unsub>;

    /**
     * The owner of a key. The key is the `KeyTypeId` + the encoded key.
     **/
    keyOwner(arg: [SpCoreCryptoKeyTypeId, Bytes]): Promise<AccountId32 | undefined>;
    keyOwner(arg: [SpCoreCryptoKeyTypeId, Bytes], callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  democracy: {
    /**
     * The number of (public) proposals that have been made so far.
     **/
    publicPropCount(): Promise<number>;
    publicPropCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The public proposals. Unsorted. The second item is the proposal.
     **/
    publicProps(): Promise<Array<[number, FrameSupportPreimagesBounded, AccountId32]>>;
    publicProps(callback: Callback<Array<[number, FrameSupportPreimagesBounded, AccountId32]>>): Promise<Unsub>;

    /**
     * Those who have locked a deposit.
     *
     * TWOX-NOTE: Safe, as increasing integer keys are safe.
     **/
    depositOf(arg: number): Promise<[Array<AccountId32>, bigint] | undefined>;
    depositOf(arg: number, callback: Callback<[Array<AccountId32>, bigint] | undefined>): Promise<Unsub>;

    /**
     * The next free referendum index, aka the number of referenda started so far.
     **/
    referendumCount(): Promise<number>;
    referendumCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The lowest referendum index representing an unbaked referendum. Equal to
     * `ReferendumCount` if there isn't a unbaked referendum.
     **/
    lowestUnbaked(): Promise<number>;
    lowestUnbaked(callback: Callback<number>): Promise<Unsub>;

    /**
     * Information concerning any given referendum.
     *
     * TWOX-NOTE: SAFE as indexes are not under an attacker’s control.
     **/
    referendumInfoOf(arg: number): Promise<PalletDemocracyReferendumInfo | undefined>;
    referendumInfoOf(arg: number, callback: Callback<PalletDemocracyReferendumInfo | undefined>): Promise<Unsub>;

    /**
     * All votes for a particular voter. We store the balance for the number of votes that we
     * have recorded. The second item is the total amount of delegations, that will be added.
     *
     * TWOX-NOTE: SAFE as `AccountId`s are crypto hashes anyway.
     **/
    votingOf(arg: AccountId32Like): Promise<PalletDemocracyVoteVoting>;
    votingOf(arg: AccountId32Like, callback: Callback<PalletDemocracyVoteVoting>): Promise<Unsub>;

    /**
     * True if the last referendum tabled was submitted externally. False if it was a public
     * proposal.
     **/
    lastTabledWasExternal(): Promise<boolean>;
    lastTabledWasExternal(callback: Callback<boolean>): Promise<Unsub>;

    /**
     * The referendum to be tabled whenever it would be valid to table an external proposal.
     * This happens when a referendum needs to be tabled and one of two conditions are met:
     * - `LastTabledWasExternal` is `false`; or
     * - `PublicProps` is empty.
     **/
    nextExternal(): Promise<[FrameSupportPreimagesBounded, PalletDemocracyVoteThreshold] | undefined>;
    nextExternal(
      callback: Callback<[FrameSupportPreimagesBounded, PalletDemocracyVoteThreshold] | undefined>,
    ): Promise<Unsub>;

    /**
     * A record of who vetoed what. Maps proposal hash to a possible existent block number
     * (until when it may not be resubmitted) and who vetoed it.
     **/
    blacklist(arg: H256): Promise<[number, Array<AccountId32>] | undefined>;
    blacklist(arg: H256, callback: Callback<[number, Array<AccountId32>] | undefined>): Promise<Unsub>;

    /**
     * Record of all proposals that have been subject to emergency cancellation.
     **/
    cancellations(arg: H256): Promise<boolean>;
    cancellations(arg: H256, callback: Callback<boolean>): Promise<Unsub>;

    /**
     * General information concerning any proposal or referendum.
     * The `Hash` refers to the preimage of the `Preimages` provider which can be a JSON
     * dump or IPFS hash of a JSON file.
     *
     * Consider a garbage collection for a metadata of finished referendums to `unrequest` (remove)
     * large preimages.
     **/
    metadataOf(arg: PalletDemocracyMetadataOwner): Promise<H256 | undefined>;
    metadataOf(arg: PalletDemocracyMetadataOwner, callback: Callback<H256 | undefined>): Promise<Unsub>;
  };
  council: {
    /**
     * The hashes of the active proposals.
     **/
    proposals(): Promise<Array<H256>>;
    proposals(callback: Callback<Array<H256>>): Promise<Unsub>;

    /**
     * Actual proposal for a given hash, if it's current.
     **/
    proposalOf(arg: H256): Promise<KitchensinkRuntimeRuntimeCall | undefined>;
    proposalOf(arg: H256, callback: Callback<KitchensinkRuntimeRuntimeCall | undefined>): Promise<Unsub>;

    /**
     * Votes on a given proposal, if it is ongoing.
     **/
    voting(arg: H256): Promise<PalletCollectiveVotes | undefined>;
    voting(arg: H256, callback: Callback<PalletCollectiveVotes | undefined>): Promise<Unsub>;

    /**
     * Proposals so far.
     **/
    proposalCount(): Promise<number>;
    proposalCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The current members of the collective. This is stored sorted (just by value).
     **/
    members(): Promise<Array<AccountId32>>;
    members(callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * The prime member that helps determine the default vote behavior in case of absentations.
     **/
    prime(): Promise<AccountId32 | undefined>;
    prime(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  technicalCommittee: {
    /**
     * The hashes of the active proposals.
     **/
    proposals(): Promise<Array<H256>>;
    proposals(callback: Callback<Array<H256>>): Promise<Unsub>;

    /**
     * Actual proposal for a given hash, if it's current.
     **/
    proposalOf(arg: H256): Promise<KitchensinkRuntimeRuntimeCall | undefined>;
    proposalOf(arg: H256, callback: Callback<KitchensinkRuntimeRuntimeCall | undefined>): Promise<Unsub>;

    /**
     * Votes on a given proposal, if it is ongoing.
     **/
    voting(arg: H256): Promise<PalletCollectiveVotes | undefined>;
    voting(arg: H256, callback: Callback<PalletCollectiveVotes | undefined>): Promise<Unsub>;

    /**
     * Proposals so far.
     **/
    proposalCount(): Promise<number>;
    proposalCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The current members of the collective. This is stored sorted (just by value).
     **/
    members(): Promise<Array<AccountId32>>;
    members(callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * The prime member that helps determine the default vote behavior in case of absentations.
     **/
    prime(): Promise<AccountId32 | undefined>;
    prime(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  elections: {
    /**
     * The current elected members.
     *
     * Invariant: Always sorted based on account id.
     **/
    members(): Promise<Array<PalletElectionsPhragmenSeatHolder>>;
    members(callback: Callback<Array<PalletElectionsPhragmenSeatHolder>>): Promise<Unsub>;

    /**
     * The current reserved runners-up.
     *
     * Invariant: Always sorted based on rank (worse to best). Upon removal of a member, the
     * last (i.e. _best_) runner-up will be replaced.
     **/
    runnersUp(): Promise<Array<PalletElectionsPhragmenSeatHolder>>;
    runnersUp(callback: Callback<Array<PalletElectionsPhragmenSeatHolder>>): Promise<Unsub>;

    /**
     * The present candidate list. A current member or runner-up can never enter this vector
     * and is always implicitly assumed to be a candidate.
     *
     * Second element is the deposit.
     *
     * Invariant: Always sorted based on account id.
     **/
    candidates(): Promise<Array<[AccountId32, bigint]>>;
    candidates(callback: Callback<Array<[AccountId32, bigint]>>): Promise<Unsub>;

    /**
     * The total number of vote rounds that have happened, excluding the upcoming one.
     **/
    electionRounds(): Promise<number>;
    electionRounds(callback: Callback<number>): Promise<Unsub>;

    /**
     * Votes and locked stake of a particular voter.
     *
     * TWOX-NOTE: SAFE as `AccountId` is a crypto hash.
     **/
    voting(arg: AccountId32Like): Promise<PalletElectionsPhragmenVoter>;
    voting(arg: AccountId32Like, callback: Callback<PalletElectionsPhragmenVoter>): Promise<Unsub>;
  };
  technicalMembership: {
    /**
     * The current membership, stored as an ordered Vec.
     **/
    members(): Promise<Array<AccountId32>>;
    members(callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * The current prime member, if one exists.
     **/
    prime(): Promise<AccountId32 | undefined>;
    prime(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  grandpa: {
    /**
     * State of the current authority set.
     **/
    state(): Promise<PalletGrandpaStoredState>;
    state(callback: Callback<PalletGrandpaStoredState>): Promise<Unsub>;

    /**
     * Pending change: (signaled at, scheduled change).
     **/
    pendingChange(): Promise<PalletGrandpaStoredPendingChange | undefined>;
    pendingChange(callback: Callback<PalletGrandpaStoredPendingChange | undefined>): Promise<Unsub>;

    /**
     * next block number where we can force a change.
     **/
    nextForced(): Promise<number | undefined>;
    nextForced(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * `true` if we are currently stalled.
     **/
    stalled(): Promise<[number, number] | undefined>;
    stalled(callback: Callback<[number, number] | undefined>): Promise<Unsub>;

    /**
     * The number of changes (both in terms of keys and underlying economic responsibilities)
     * in the "set" of Grandpa validators from genesis.
     **/
    currentSetId(): Promise<bigint>;
    currentSetId(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * A mapping from grandpa set ID to the index of the *most recent* session for which its
     * members were responsible.
     *
     * This is only used for validating equivocation proofs. An equivocation proof must
     * contains a key-ownership proof for a given session, therefore we need a way to tie
     * together sessions and GRANDPA set ids, i.e. we need to validate that a validator
     * was the owner of a given key on a given session, and what the active set ID was
     * during that session.
     *
     * TWOX-NOTE: `SetId` is not under user control.
     **/
    setIdSession(arg: bigint): Promise<number | undefined>;
    setIdSession(arg: bigint, callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The current list of authorities.
     **/
    authorities(): Promise<Array<[SpConsensusGrandpaAppPublic, bigint]>>;
    authorities(callback: Callback<Array<[SpConsensusGrandpaAppPublic, bigint]>>): Promise<Unsub>;
  };
  treasury: {
    /**
     * Number of proposals that have been made.
     **/
    proposalCount(): Promise<number>;
    proposalCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Proposals that have been made.
     **/
    proposals(arg: number): Promise<PalletTreasuryProposal | undefined>;
    proposals(arg: number, callback: Callback<PalletTreasuryProposal | undefined>): Promise<Unsub>;

    /**
     * The amount which has been reported as inactive to Currency.
     **/
    deactivated(): Promise<bigint>;
    deactivated(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Proposal indices that have been approved but not yet awarded.
     **/
    approvals(): Promise<Array<number>>;
    approvals(callback: Callback<Array<number>>): Promise<Unsub>;

    /**
     * The count of spends that have been made.
     **/
    spendCount(): Promise<number>;
    spendCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Spends that have been approved and being processed.
     **/
    spends(arg: number): Promise<PalletTreasurySpendStatus | undefined>;
    spends(arg: number, callback: Callback<PalletTreasurySpendStatus | undefined>): Promise<Unsub>;
  };
  assetRate: {
    /**
     * Maps an asset to its fixed point representation in the native balance.
     *
     * E.g. `native_amount = asset_amount * ConversionRateToNative::<T>::get(asset_kind)`
     **/
    conversionRateToNative(arg: number): Promise<FixedU128 | undefined>;
    conversionRateToNative(arg: number, callback: Callback<FixedU128 | undefined>): Promise<Unsub>;
  };
  contracts: {
    /**
     * A mapping from a contract's code hash to its code.
     **/
    pristineCode(arg: H256): Promise<Bytes | undefined>;
    pristineCode(arg: H256, callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * A mapping from a contract's code hash to its code info.
     **/
    codeInfoOf(arg: H256): Promise<PalletContractsWasmCodeInfo | undefined>;
    codeInfoOf(arg: H256, callback: Callback<PalletContractsWasmCodeInfo | undefined>): Promise<Unsub>;

    /**
     * This is a **monotonic** counter incremented on contract instantiation.
     *
     * This is used in order to generate unique trie ids for contracts.
     * The trie id of a new contract is calculated from hash(account_id, nonce).
     * The nonce is required because otherwise the following sequence would lead to
     * a possible collision of storage:
     *
     * 1. Create a new contract.
     * 2. Terminate the contract.
     * 3. Immediately recreate the contract with the same account_id.
     *
     * This is bad because the contents of a trie are deleted lazily and there might be
     * storage of the old instantiation still in it when the new contract is created. Please
     * note that we can't replace the counter by the block number because the sequence above
     * can happen in the same block. We also can't keep the account counter in memory only
     * because storage is the only way to communicate across different extrinsics in the
     * same block.
     *
     * # Note
     *
     * Do not use it to determine the number of contracts. It won't be decremented if
     * a contract is destroyed.
     **/
    nonce(): Promise<bigint>;
    nonce(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * The code associated with a given account.
     *
     * TWOX-NOTE: SAFE since `AccountId` is a secure hash.
     **/
    contractInfoOf(arg: AccountId32Like): Promise<PalletContractsStorageContractInfo | undefined>;
    contractInfoOf(
      arg: AccountId32Like,
      callback: Callback<PalletContractsStorageContractInfo | undefined>,
    ): Promise<Unsub>;

    /**
     * Evicted contracts that await child trie deletion.
     *
     * Child trie deletion is a heavy operation depending on the amount of storage items
     * stored in said trie. Therefore this operation is performed lazily in `on_idle`.
     **/
    deletionQueue(arg: number): Promise<Bytes | undefined>;
    deletionQueue(arg: number, callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * A pair of monotonic counters used to track the latest contract marked for deletion
     * and the latest deleted contract in queue.
     **/
    deletionQueueCounter(): Promise<PalletContractsStorageDeletionQueueManager>;
    deletionQueueCounter(callback: Callback<PalletContractsStorageDeletionQueueManager>): Promise<Unsub>;

    /**
     * A migration can span across multiple blocks. This storage defines a cursor to track the
     * progress of the migration, enabling us to resume from the last completed position.
     **/
    migrationInProgress(): Promise<Bytes | undefined>;
    migrationInProgress(callback: Callback<Bytes | undefined>): Promise<Unsub>;
  };
  sudo: {
    /**
     * The `AccountId` of the sudo key.
     **/
    key(): Promise<AccountId32 | undefined>;
    key(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  imOnline: {
    /**
     * The block number after which it's ok to send heartbeats in the current
     * session.
     *
     * At the beginning of each session we set this to a value that should fall
     * roughly in the middle of the session duration. The idea is to first wait for
     * the validators to produce a block in the current session, so that the
     * heartbeat later on will not be necessary.
     *
     * This value will only be used as a fallback if we fail to get a proper session
     * progress estimate from `NextSessionRotation`, as those estimates should be
     * more accurate then the value we calculate for `HeartbeatAfter`.
     **/
    heartbeatAfter(): Promise<number>;
    heartbeatAfter(callback: Callback<number>): Promise<Unsub>;

    /**
     * The current set of keys that may issue a heartbeat.
     **/
    keys(): Promise<Array<PalletImOnlineSr25519AppSr25519Public>>;
    keys(callback: Callback<Array<PalletImOnlineSr25519AppSr25519Public>>): Promise<Unsub>;

    /**
     * For each session index, we keep a mapping of `SessionIndex` and `AuthIndex`.
     **/
    receivedHeartbeats(arg: [number, number]): Promise<boolean | undefined>;
    receivedHeartbeats(arg: [number, number], callback: Callback<boolean | undefined>): Promise<Unsub>;

    /**
     * For each session index, we keep a mapping of `ValidatorId<T>` to the
     * number of blocks authored by the given authority.
     **/
    authoredBlocks(arg: [number, AccountId32Like]): Promise<number>;
    authoredBlocks(arg: [number, AccountId32Like], callback: Callback<number>): Promise<Unsub>;
  };
  authorityDiscovery: {
    /**
     * Keys of the current authority set.
     **/
    keys(): Promise<Array<SpAuthorityDiscoveryAppPublic>>;
    keys(callback: Callback<Array<SpAuthorityDiscoveryAppPublic>>): Promise<Unsub>;

    /**
     * Keys of the next authority set.
     **/
    nextKeys(): Promise<Array<SpAuthorityDiscoveryAppPublic>>;
    nextKeys(callback: Callback<Array<SpAuthorityDiscoveryAppPublic>>): Promise<Unsub>;
  };
  offences: {
    /**
     * The primary structure that holds all offence records keyed by report identifiers.
     **/
    reports(arg: H256): Promise<SpStakingOffenceOffenceDetails | undefined>;
    reports(arg: H256, callback: Callback<SpStakingOffenceOffenceDetails | undefined>): Promise<Unsub>;

    /**
     * A vector of reports of the same kind that happened at the same time slot.
     **/
    concurrentReportsIndex(arg: [FixedBytes<16>, Bytes]): Promise<Array<H256>>;
    concurrentReportsIndex(arg: [FixedBytes<16>, Bytes], callback: Callback<Array<H256>>): Promise<Unsub>;
  };
  randomnessCollectiveFlip: {
    /**
     * Series of block headers from the last 81 blocks that acts as random seed material. This
     * is arranged as a ring buffer with `block_number % 81` being the index into the `Vec` of
     * the oldest hash.
     **/
    randomMaterial(): Promise<Array<H256>>;
    randomMaterial(callback: Callback<Array<H256>>): Promise<Unsub>;
  };
  identity: {
    /**
     * Information that is pertinent to identify the entity behind an account.
     *
     * TWOX-NOTE: OK ― `AccountId` is a secure hash.
     **/
    identityOf(arg: AccountId32Like): Promise<PalletIdentityRegistration | undefined>;
    identityOf(arg: AccountId32Like, callback: Callback<PalletIdentityRegistration | undefined>): Promise<Unsub>;

    /**
     * The super-identity of an alternative "sub" identity together with its name, within that
     * context. If the account is not some other account's sub-identity, then just `None`.
     **/
    superOf(arg: AccountId32Like): Promise<[AccountId32, Data] | undefined>;
    superOf(arg: AccountId32Like, callback: Callback<[AccountId32, Data] | undefined>): Promise<Unsub>;

    /**
     * Alternative "sub" identities of this account.
     *
     * The first item is the deposit, the second is a vector of the accounts.
     *
     * TWOX-NOTE: OK ― `AccountId` is a secure hash.
     **/
    subsOf(arg: AccountId32Like): Promise<[bigint, Array<AccountId32>]>;
    subsOf(arg: AccountId32Like, callback: Callback<[bigint, Array<AccountId32>]>): Promise<Unsub>;

    /**
     * The set of registrars. Not expected to get very big as can only be added through a
     * special origin (likely a council motion).
     *
     * The index into this can be cast to `RegistrarIndex` to get a valid value.
     **/
    registrars(): Promise<Array<PalletIdentityRegistrarInfo | undefined>>;
    registrars(callback: Callback<Array<PalletIdentityRegistrarInfo | undefined>>): Promise<Unsub>;
  };
  society: {
    /**
     * The max number of members for the society at one time.
     **/
    parameters(): Promise<PalletSocietyGroupParams | undefined>;
    parameters(callback: Callback<PalletSocietyGroupParams | undefined>): Promise<Unsub>;

    /**
     * Amount of our account balance that is specifically for the next round's bid(s).
     **/
    pot(): Promise<bigint>;
    pot(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * The first member.
     **/
    founder(): Promise<AccountId32 | undefined>;
    founder(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;

    /**
     * The most primary from the most recently approved rank 0 members in the society.
     **/
    head(): Promise<AccountId32 | undefined>;
    head(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;

    /**
     * A hash of the rules of this society concerning membership. Can only be set once and
     * only by the founder.
     **/
    rules(): Promise<H256 | undefined>;
    rules(callback: Callback<H256 | undefined>): Promise<Unsub>;

    /**
     * The current members and their rank. Doesn't include `SuspendedMembers`.
     **/
    members(arg: AccountId32Like): Promise<PalletSocietyMemberRecord | undefined>;
    members(arg: AccountId32Like, callback: Callback<PalletSocietyMemberRecord | undefined>): Promise<Unsub>;

    /**
     * Information regarding rank-0 payouts, past and future.
     **/
    payouts(arg: AccountId32Like): Promise<PalletSocietyPayoutRecord>;
    payouts(arg: AccountId32Like, callback: Callback<PalletSocietyPayoutRecord>): Promise<Unsub>;

    /**
     * The number of items in `Members` currently. (Doesn't include `SuspendedMembers`.)
     **/
    memberCount(): Promise<number>;
    memberCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The current items in `Members` keyed by their unique index. Keys are densely populated
     * `0..MemberCount` (does not include `MemberCount`).
     **/
    memberByIndex(arg: number): Promise<AccountId32 | undefined>;
    memberByIndex(arg: number, callback: Callback<AccountId32 | undefined>): Promise<Unsub>;

    /**
     * The set of suspended members, with their old membership record.
     **/
    suspendedMembers(arg: AccountId32Like): Promise<PalletSocietyMemberRecord | undefined>;
    suspendedMembers(arg: AccountId32Like, callback: Callback<PalletSocietyMemberRecord | undefined>): Promise<Unsub>;

    /**
     * The number of rounds which have passed.
     **/
    roundCount(): Promise<number>;
    roundCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The current bids, stored ordered by the value of the bid.
     **/
    bids(): Promise<Array<PalletSocietyBid>>;
    bids(callback: Callback<Array<PalletSocietyBid>>): Promise<Unsub>;
    candidates(arg: AccountId32Like): Promise<PalletSocietyCandidacy | undefined>;
    candidates(arg: AccountId32Like, callback: Callback<PalletSocietyCandidacy | undefined>): Promise<Unsub>;

    /**
     * The current skeptic.
     **/
    skeptic(): Promise<AccountId32 | undefined>;
    skeptic(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;

    /**
     * Double map from Candidate -> Voter -> (Maybe) Vote.
     **/
    votes(arg: [AccountId32Like, AccountId32Like]): Promise<PalletSocietyVote | undefined>;
    votes(arg: [AccountId32Like, AccountId32Like], callback: Callback<PalletSocietyVote | undefined>): Promise<Unsub>;

    /**
     * Clear-cursor for Vote, map from Candidate -> (Maybe) Cursor.
     **/
    voteClearCursor(arg: AccountId32Like): Promise<Bytes | undefined>;
    voteClearCursor(arg: AccountId32Like, callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * At the end of the claim period, this contains the most recently approved members (along with
     * their bid and round ID) who is from the most recent round with the lowest bid. They will
     * become the new `Head`.
     **/
    nextHead(): Promise<PalletSocietyIntakeRecord | undefined>;
    nextHead(callback: Callback<PalletSocietyIntakeRecord | undefined>): Promise<Unsub>;

    /**
     * The number of challenge rounds there have been. Used to identify stale DefenderVotes.
     **/
    challengeRoundCount(): Promise<number>;
    challengeRoundCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The defending member currently being challenged, along with a running tally of votes.
     **/
    defending(): Promise<[AccountId32, AccountId32, PalletSocietyTally] | undefined>;
    defending(callback: Callback<[AccountId32, AccountId32, PalletSocietyTally] | undefined>): Promise<Unsub>;

    /**
     * Votes for the defender, keyed by challenge round.
     **/
    defenderVotes(arg: [number, AccountId32Like]): Promise<PalletSocietyVote | undefined>;
    defenderVotes(arg: [number, AccountId32Like], callback: Callback<PalletSocietyVote | undefined>): Promise<Unsub>;
  };
  recovery: {
    /**
     * The set of recoverable accounts and their recovery configuration.
     **/
    recoverable(arg: AccountId32Like): Promise<PalletRecoveryRecoveryConfig | undefined>;
    recoverable(arg: AccountId32Like, callback: Callback<PalletRecoveryRecoveryConfig | undefined>): Promise<Unsub>;

    /**
     * Active recovery attempts.
     *
     * First account is the account to be recovered, and the second account
     * is the user trying to recover the account.
     **/
    activeRecoveries(arg: [AccountId32Like, AccountId32Like]): Promise<PalletRecoveryActiveRecovery | undefined>;
    activeRecoveries(
      arg: [AccountId32Like, AccountId32Like],
      callback: Callback<PalletRecoveryActiveRecovery | undefined>,
    ): Promise<Unsub>;

    /**
     * The list of allowed proxy accounts.
     *
     * Map from the user who can access it to the recovered account.
     **/
    proxy(arg: AccountId32Like): Promise<AccountId32 | undefined>;
    proxy(arg: AccountId32Like, callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  vesting: {
    /**
     * Information regarding the vesting of a given account.
     **/
    vesting(arg: AccountId32Like): Promise<Array<PalletVestingVestingInfo> | undefined>;
    vesting(arg: AccountId32Like, callback: Callback<Array<PalletVestingVestingInfo> | undefined>): Promise<Unsub>;

    /**
     * Storage version of the pallet.
     *
     * New networks start with latest version, as determined by the genesis build.
     **/
    storageVersion(): Promise<PalletVestingReleases>;
    storageVersion(callback: Callback<PalletVestingReleases>): Promise<Unsub>;
  };
  scheduler: {
    incompleteSince(): Promise<number | undefined>;
    incompleteSince(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Items to be executed, indexed by the block number that they should be executed on.
     **/
    agenda(arg: number): Promise<Array<PalletSchedulerScheduled | undefined>>;
    agenda(arg: number, callback: Callback<Array<PalletSchedulerScheduled | undefined>>): Promise<Unsub>;

    /**
     * Lookup from a name to the block number and index of the task.
     *
     * For v3 -> v4 the previously unbounded identities are Blake2-256 hashed to form the v4
     * identities.
     **/
    lookup(arg: FixedBytes<32>): Promise<[number, number] | undefined>;
    lookup(arg: FixedBytes<32>, callback: Callback<[number, number] | undefined>): Promise<Unsub>;
  };
  glutton: {
    /**
     * The proportion of the remaining `ref_time` to consume during `on_idle`.
     *
     * `1.0` is mapped to `100%`. Must be at most [`crate::RESOURCE_HARD_LIMIT`]. Setting this to
     * over `1.0` could stall the chain.
     **/
    compute(): Promise<FixedU64>;
    compute(callback: Callback<FixedU64>): Promise<Unsub>;

    /**
     * The proportion of the remaining `proof_size` to consume during `on_idle`.
     *
     * `1.0` is mapped to `100%`. Must be at most [`crate::RESOURCE_HARD_LIMIT`]. Setting this to
     * over `1.0` could stall the chain.
     **/
    storage(): Promise<FixedU64>;
    storage(callback: Callback<FixedU64>): Promise<Unsub>;

    /**
     * Storage map used for wasting proof size.
     *
     * It contains no meaningful data - hence the name "Trash". The maximal number of entries is
     * set to 65k, which is just below the next jump at 16^4. This is important to reduce the proof
     * size benchmarking overestimate. The assumption here is that we won't have more than 65k *
     * 1KiB = 65MiB of proof size wasting in practice. However, this limit is not enforced, so the
     * pallet would also work out of the box with more entries, but its benchmarked proof weight
     * would possibly be underestimated in that case.
     **/
    trashData(arg: number): Promise<FixedBytes<1024> | undefined>;
    trashData(arg: number, callback: Callback<FixedBytes<1024> | undefined>): Promise<Unsub>;

    /**
     * The current number of entries in `TrashData`.
     **/
    trashDataCount(): Promise<number>;
    trashDataCount(callback: Callback<number>): Promise<Unsub>;
  };
  preimage: {
    /**
     * The request status of a given hash.
     **/
    statusFor(arg: H256): Promise<PalletPreimageOldRequestStatus | undefined>;
    statusFor(arg: H256, callback: Callback<PalletPreimageOldRequestStatus | undefined>): Promise<Unsub>;

    /**
     * The request status of a given hash.
     **/
    requestStatusFor(arg: H256): Promise<PalletPreimageRequestStatus | undefined>;
    requestStatusFor(arg: H256, callback: Callback<PalletPreimageRequestStatus | undefined>): Promise<Unsub>;
    preimageFor(arg: [H256, number]): Promise<Bytes | undefined>;
    preimageFor(arg: [H256, number], callback: Callback<Bytes | undefined>): Promise<Unsub>;
  };
  proxy: {
    /**
     * The set of account proxies. Maps the account which has delegated to the accounts
     * which are being delegated to, together with the amount held on deposit.
     **/
    proxies(arg: AccountId32Like): Promise<[Array<PalletProxyProxyDefinition>, bigint]>;
    proxies(arg: AccountId32Like, callback: Callback<[Array<PalletProxyProxyDefinition>, bigint]>): Promise<Unsub>;

    /**
     * The announcements made by the proxy (key).
     **/
    announcements(arg: AccountId32Like): Promise<[Array<PalletProxyAnnouncement>, bigint]>;
    announcements(arg: AccountId32Like, callback: Callback<[Array<PalletProxyAnnouncement>, bigint]>): Promise<Unsub>;
  };
  multisig: {
    /**
     * The set of open multisig operations.
     **/
    multisigs(arg: [AccountId32Like, FixedBytes<32>]): Promise<PalletMultisigMultisig | undefined>;
    multisigs(
      arg: [AccountId32Like, FixedBytes<32>],
      callback: Callback<PalletMultisigMultisig | undefined>,
    ): Promise<Unsub>;
  };
  bounties: {
    /**
     * Number of bounty proposals that have been made.
     **/
    bountyCount(): Promise<number>;
    bountyCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Bounties that have been made.
     **/
    bounties(arg: number): Promise<PalletBountiesBounty | undefined>;
    bounties(arg: number, callback: Callback<PalletBountiesBounty | undefined>): Promise<Unsub>;

    /**
     * The description of each bounty.
     **/
    bountyDescriptions(arg: number): Promise<Bytes | undefined>;
    bountyDescriptions(arg: number, callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * Bounty indices that have been approved but not yet funded.
     **/
    bountyApprovals(): Promise<Array<number>>;
    bountyApprovals(callback: Callback<Array<number>>): Promise<Unsub>;
  };
  tips: {
    /**
     * TipsMap that are not yet completed. Keyed by the hash of `(reason, who)` from the value.
     * This has the insecure enumerable hash function since the key itself is already
     * guaranteed to be a secure hash.
     **/
    tips(arg: H256): Promise<PalletTipsOpenTip | undefined>;
    tips(arg: H256, callback: Callback<PalletTipsOpenTip | undefined>): Promise<Unsub>;

    /**
     * Simple preimage lookup from the reason's hash to the original data. Again, has an
     * insecure enumerable hash since the key is guaranteed to be the result of a secure hash.
     **/
    reasons(arg: H256): Promise<Bytes | undefined>;
    reasons(arg: H256, callback: Callback<Bytes | undefined>): Promise<Unsub>;
  };
  assets: {
    /**
     * Details of an asset.
     **/
    asset(arg: number): Promise<PalletAssetsAssetDetails | undefined>;
    asset(arg: number, callback: Callback<PalletAssetsAssetDetails | undefined>): Promise<Unsub>;

    /**
     * The holdings of a specific account for a specific asset.
     **/
    account(arg: [number, AccountId32Like]): Promise<PalletAssetsAssetAccount | undefined>;
    account(arg: [number, AccountId32Like], callback: Callback<PalletAssetsAssetAccount | undefined>): Promise<Unsub>;

    /**
     * Approved balance transfers. First balance is the amount approved for transfer. Second
     * is the amount of `T::Currency` reserved for storing this.
     * First key is the asset ID, second key is the owner and third key is the delegate.
     **/
    approvals(arg: [number, AccountId32Like, AccountId32Like]): Promise<PalletAssetsApproval | undefined>;
    approvals(
      arg: [number, AccountId32Like, AccountId32Like],
      callback: Callback<PalletAssetsApproval | undefined>,
    ): Promise<Unsub>;

    /**
     * Metadata of an asset.
     **/
    metadata(arg: number): Promise<PalletAssetsAssetMetadata>;
    metadata(arg: number, callback: Callback<PalletAssetsAssetMetadata>): Promise<Unsub>;
  };
  poolAssets: {
    /**
     * Details of an asset.
     **/
    asset(arg: number): Promise<PalletAssetsAssetDetails | undefined>;
    asset(arg: number, callback: Callback<PalletAssetsAssetDetails | undefined>): Promise<Unsub>;

    /**
     * The holdings of a specific account for a specific asset.
     **/
    account(arg: [number, AccountId32Like]): Promise<PalletAssetsAssetAccount | undefined>;
    account(arg: [number, AccountId32Like], callback: Callback<PalletAssetsAssetAccount | undefined>): Promise<Unsub>;

    /**
     * Approved balance transfers. First balance is the amount approved for transfer. Second
     * is the amount of `T::Currency` reserved for storing this.
     * First key is the asset ID, second key is the owner and third key is the delegate.
     **/
    approvals(arg: [number, AccountId32Like, AccountId32Like]): Promise<PalletAssetsApproval | undefined>;
    approvals(
      arg: [number, AccountId32Like, AccountId32Like],
      callback: Callback<PalletAssetsApproval | undefined>,
    ): Promise<Unsub>;

    /**
     * Metadata of an asset.
     **/
    metadata(arg: number): Promise<PalletAssetsAssetMetadata>;
    metadata(arg: number, callback: Callback<PalletAssetsAssetMetadata>): Promise<Unsub>;
  };
  mmr: {
    /**
     * Latest MMR Root hash.
     **/
    rootHash(): Promise<H256>;
    rootHash(callback: Callback<H256>): Promise<Unsub>;

    /**
     * Current size of the MMR (number of leaves).
     **/
    numberOfLeaves(): Promise<bigint>;
    numberOfLeaves(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Hashes of the nodes in the MMR.
     *
     * Note this collection only contains MMR peaks, the inner nodes (and leaves)
     * are pruned and only stored in the Offchain DB.
     **/
    nodes(arg: bigint): Promise<H256 | undefined>;
    nodes(arg: bigint, callback: Callback<H256 | undefined>): Promise<Unsub>;
  };
  lottery: {
    lotteryIndex(): Promise<number>;
    lotteryIndex(callback: Callback<number>): Promise<Unsub>;

    /**
     * The configuration for the current lottery.
     **/
    lottery(): Promise<PalletLotteryLotteryConfig | undefined>;
    lottery(callback: Callback<PalletLotteryLotteryConfig | undefined>): Promise<Unsub>;

    /**
     * Users who have purchased a ticket. (Lottery Index, Tickets Purchased)
     **/
    participants(arg: AccountId32Like): Promise<[number, Array<[number, number]>]>;
    participants(arg: AccountId32Like, callback: Callback<[number, Array<[number, number]>]>): Promise<Unsub>;

    /**
     * Total number of tickets sold.
     **/
    ticketsCount(): Promise<number>;
    ticketsCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Each ticket's owner.
     *
     * May have residual storage from previous lotteries. Use `TicketsCount` to see which ones
     * are actually valid ticket mappings.
     **/
    tickets(arg: number): Promise<AccountId32 | undefined>;
    tickets(arg: number, callback: Callback<AccountId32 | undefined>): Promise<Unsub>;

    /**
     * The calls stored in this pallet to be used in an active lottery if configured
     * by `Config::ValidateCall`.
     **/
    callIndices(): Promise<Array<[number, number]>>;
    callIndices(callback: Callback<Array<[number, number]>>): Promise<Unsub>;
  };
  nis: {
    /**
     * The totals of items and balances within each queue. Saves a lot of storage reads in the
     * case of sparsely packed queues.
     *
     * The vector is indexed by duration in `Period`s, offset by one, so information on the queue
     * whose duration is one `Period` would be storage `0`.
     **/
    queueTotals(): Promise<Array<[number, bigint]>>;
    queueTotals(callback: Callback<Array<[number, bigint]>>): Promise<Unsub>;

    /**
     * The queues of bids. Indexed by duration (in `Period`s).
     **/
    queues(arg: number): Promise<Array<PalletNisBid>>;
    queues(arg: number, callback: Callback<Array<PalletNisBid>>): Promise<Unsub>;

    /**
     * Summary information over the general state.
     **/
    summary(): Promise<PalletNisSummaryRecord>;
    summary(callback: Callback<PalletNisSummaryRecord>): Promise<Unsub>;

    /**
     * The currently outstanding receipts, indexed according to the order of creation.
     **/
    receipts(arg: number): Promise<PalletNisReceiptRecord | undefined>;
    receipts(arg: number, callback: Callback<PalletNisReceiptRecord | undefined>): Promise<Unsub>;
  };
  uniques: {
    /**
     * Details of a collection.
     **/
    class(arg: number): Promise<PalletUniquesCollectionDetails | undefined>;
    class(arg: number, callback: Callback<PalletUniquesCollectionDetails | undefined>): Promise<Unsub>;

    /**
     * The collection, if any, of which an account is willing to take ownership.
     **/
    ownershipAcceptance(arg: AccountId32Like): Promise<number | undefined>;
    ownershipAcceptance(arg: AccountId32Like, callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The items held by any given account; set out this way so that items owned by a single
     * account can be enumerated.
     **/
    account(arg: [AccountId32Like, number, number]): Promise<[] | undefined>;
    account(arg: [AccountId32Like, number, number], callback: Callback<[] | undefined>): Promise<Unsub>;

    /**
     * The collections owned by any given account; set out this way so that collections owned by
     * a single account can be enumerated.
     **/
    classAccount(arg: [AccountId32Like, number]): Promise<[] | undefined>;
    classAccount(arg: [AccountId32Like, number], callback: Callback<[] | undefined>): Promise<Unsub>;

    /**
     * The items in existence and their ownership details.
     **/
    asset(arg: [number, number]): Promise<PalletUniquesItemDetails | undefined>;
    asset(arg: [number, number], callback: Callback<PalletUniquesItemDetails | undefined>): Promise<Unsub>;

    /**
     * Metadata of a collection.
     **/
    classMetadataOf(arg: number): Promise<PalletUniquesCollectionMetadata | undefined>;
    classMetadataOf(arg: number, callback: Callback<PalletUniquesCollectionMetadata | undefined>): Promise<Unsub>;

    /**
     * Metadata of an item.
     **/
    instanceMetadataOf(arg: [number, number]): Promise<PalletUniquesItemMetadata | undefined>;
    instanceMetadataOf(
      arg: [number, number],
      callback: Callback<PalletUniquesItemMetadata | undefined>,
    ): Promise<Unsub>;

    /**
     * Attributes of a collection.
     **/
    attribute(arg: [number, number | undefined, Bytes]): Promise<[Bytes, bigint] | undefined>;
    attribute(
      arg: [number, number | undefined, Bytes],
      callback: Callback<[Bytes, bigint] | undefined>,
    ): Promise<Unsub>;

    /**
     * Price of an asset instance.
     **/
    itemPriceOf(arg: [number, number]): Promise<[bigint, AccountId32 | undefined] | undefined>;
    itemPriceOf(
      arg: [number, number],
      callback: Callback<[bigint, AccountId32 | undefined] | undefined>,
    ): Promise<Unsub>;

    /**
     * Keeps track of the number of items a collection might have.
     **/
    collectionMaxSupply(arg: number): Promise<number | undefined>;
    collectionMaxSupply(arg: number, callback: Callback<number | undefined>): Promise<Unsub>;
  };
  nfts: {
    /**
     * Details of a collection.
     **/
    collection(arg: number): Promise<PalletNftsCollectionDetails | undefined>;
    collection(arg: number, callback: Callback<PalletNftsCollectionDetails | undefined>): Promise<Unsub>;

    /**
     * The collection, if any, of which an account is willing to take ownership.
     **/
    ownershipAcceptance(arg: AccountId32Like): Promise<number | undefined>;
    ownershipAcceptance(arg: AccountId32Like, callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The items held by any given account; set out this way so that items owned by a single
     * account can be enumerated.
     **/
    account(arg: [AccountId32Like, number, number]): Promise<[] | undefined>;
    account(arg: [AccountId32Like, number, number], callback: Callback<[] | undefined>): Promise<Unsub>;

    /**
     * The collections owned by any given account; set out this way so that collections owned by
     * a single account can be enumerated.
     **/
    collectionAccount(arg: [AccountId32Like, number]): Promise<[] | undefined>;
    collectionAccount(arg: [AccountId32Like, number], callback: Callback<[] | undefined>): Promise<Unsub>;

    /**
     * The items in existence and their ownership details.
     * Stores collection roles as per account.
     **/
    collectionRoleOf(arg: [number, AccountId32Like]): Promise<PalletNftsBitFlagsCollectionRole | undefined>;
    collectionRoleOf(
      arg: [number, AccountId32Like],
      callback: Callback<PalletNftsBitFlagsCollectionRole | undefined>,
    ): Promise<Unsub>;

    /**
     * The items in existence and their ownership details.
     **/
    item(arg: [number, number]): Promise<PalletNftsItemDetails | undefined>;
    item(arg: [number, number], callback: Callback<PalletNftsItemDetails | undefined>): Promise<Unsub>;

    /**
     * Metadata of a collection.
     **/
    collectionMetadataOf(arg: number): Promise<PalletNftsCollectionMetadata | undefined>;
    collectionMetadataOf(arg: number, callback: Callback<PalletNftsCollectionMetadata | undefined>): Promise<Unsub>;

    /**
     * Metadata of an item.
     **/
    itemMetadataOf(arg: [number, number]): Promise<PalletNftsItemMetadata | undefined>;
    itemMetadataOf(arg: [number, number], callback: Callback<PalletNftsItemMetadata | undefined>): Promise<Unsub>;

    /**
     * Attributes of a collection.
     **/
    attribute(
      arg: [number, number | undefined, PalletNftsAttributeNamespace, Bytes],
    ): Promise<[Bytes, PalletNftsAttributeDeposit] | undefined>;
    attribute(
      arg: [number, number | undefined, PalletNftsAttributeNamespace, Bytes],
      callback: Callback<[Bytes, PalletNftsAttributeDeposit] | undefined>,
    ): Promise<Unsub>;

    /**
     * A price of an item.
     **/
    itemPriceOf(arg: [number, number]): Promise<[bigint, AccountId32 | undefined] | undefined>;
    itemPriceOf(
      arg: [number, number],
      callback: Callback<[bigint, AccountId32 | undefined] | undefined>,
    ): Promise<Unsub>;

    /**
     * Item attribute approvals.
     **/
    itemAttributesApprovalsOf(arg: [number, number]): Promise<Array<AccountId32>>;
    itemAttributesApprovalsOf(arg: [number, number], callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * Stores the `CollectionId` that is going to be used for the next collection.
     * This gets incremented whenever a new collection is created.
     **/
    nextCollectionId(): Promise<number | undefined>;
    nextCollectionId(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Handles all the pending swaps.
     **/
    pendingSwapOf(arg: [number, number]): Promise<PalletNftsPendingSwap | undefined>;
    pendingSwapOf(arg: [number, number], callback: Callback<PalletNftsPendingSwap | undefined>): Promise<Unsub>;

    /**
     * Config of a collection.
     **/
    collectionConfigOf(arg: number): Promise<PalletNftsCollectionConfig | undefined>;
    collectionConfigOf(arg: number, callback: Callback<PalletNftsCollectionConfig | undefined>): Promise<Unsub>;

    /**
     * Config of an item.
     **/
    itemConfigOf(arg: [number, number]): Promise<PalletNftsItemConfig | undefined>;
    itemConfigOf(arg: [number, number], callback: Callback<PalletNftsItemConfig | undefined>): Promise<Unsub>;
  };
  nftFractionalization: {
    /**
     * Keeps track of the corresponding NFT ID, asset ID and amount minted.
     **/
    nftToAsset(arg: [number, number]): Promise<PalletNftFractionalizationDetails | undefined>;
    nftToAsset(
      arg: [number, number],
      callback: Callback<PalletNftFractionalizationDetails | undefined>,
    ): Promise<Unsub>;
  };
  salary: {
    /**
     * The overall status of the system.
     **/
    status(): Promise<PalletSalaryStatusType | undefined>;
    status(callback: Callback<PalletSalaryStatusType | undefined>): Promise<Unsub>;

    /**
     * The status of a claimant.
     **/
    claimant(arg: AccountId32Like): Promise<PalletSalaryClaimantStatus | undefined>;
    claimant(arg: AccountId32Like, callback: Callback<PalletSalaryClaimantStatus | undefined>): Promise<Unsub>;
  };
  coreFellowship: {
    /**
     * The overall status of the system.
     **/
    params(): Promise<PalletCoreFellowshipParamsType>;
    params(callback: Callback<PalletCoreFellowshipParamsType>): Promise<Unsub>;

    /**
     * The status of a claimant.
     **/
    member(arg: AccountId32Like): Promise<PalletCoreFellowshipMemberStatus | undefined>;
    member(arg: AccountId32Like, callback: Callback<PalletCoreFellowshipMemberStatus | undefined>): Promise<Unsub>;

    /**
     * Some evidence together with the desired outcome for which it was presented.
     **/
    memberEvidence(arg: AccountId32Like): Promise<[PalletCoreFellowshipWish, Bytes] | undefined>;
    memberEvidence(
      arg: AccountId32Like,
      callback: Callback<[PalletCoreFellowshipWish, Bytes] | undefined>,
    ): Promise<Unsub>;
  };
  transactionStorage: {
    /**
     * Collection of transaction metadata by block number.
     **/
    transactions(arg: number): Promise<Array<PalletTransactionStorageTransactionInfo> | undefined>;
    transactions(
      arg: number,
      callback: Callback<Array<PalletTransactionStorageTransactionInfo> | undefined>,
    ): Promise<Unsub>;

    /**
     * Count indexed chunks for each block.
     **/
    chunkCount(arg: number): Promise<number>;
    chunkCount(arg: number, callback: Callback<number>): Promise<Unsub>;

    /**
     * Storage fee per byte.
     **/
    byteFee(): Promise<bigint | undefined>;
    byteFee(callback: Callback<bigint | undefined>): Promise<Unsub>;

    /**
     * Storage fee per transaction.
     **/
    entryFee(): Promise<bigint | undefined>;
    entryFee(callback: Callback<bigint | undefined>): Promise<Unsub>;

    /**
     * Storage period for data in blocks. Should match `sp_storage_proof::DEFAULT_STORAGE_PERIOD`
     * for block authoring.
     **/
    storagePeriod(): Promise<number>;
    storagePeriod(callback: Callback<number>): Promise<Unsub>;
    blockTransactions(): Promise<Array<PalletTransactionStorageTransactionInfo>>;
    blockTransactions(callback: Callback<Array<PalletTransactionStorageTransactionInfo>>): Promise<Unsub>;

    /**
     * Was the proof checked in this block?
     **/
    proofChecked(): Promise<boolean>;
    proofChecked(callback: Callback<boolean>): Promise<Unsub>;
  };
  voterList: {
    /**
     * A single node, within some bag.
     *
     * Nodes store links forward and back within their respective bags.
     **/
    listNodes(arg: AccountId32Like): Promise<PalletBagsListListNode | undefined>;
    listNodes(arg: AccountId32Like, callback: Callback<PalletBagsListListNode | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForListNodes(): Promise<number>;
    counterForListNodes(callback: Callback<number>): Promise<Unsub>;

    /**
     * A bag stored in storage.
     *
     * Stores a `Bag` struct, which stores head and tail pointers to itself.
     **/
    listBags(arg: bigint): Promise<PalletBagsListListBag | undefined>;
    listBags(arg: bigint, callback: Callback<PalletBagsListListBag | undefined>): Promise<Unsub>;
  };
  stateTrieMigration: {
    /**
     * Migration progress.
     *
     * This stores the snapshot of the last migrated keys. It can be set into motion and move
     * forward by any of the means provided by this pallet.
     **/
    migrationProcess(): Promise<PalletStateTrieMigrationMigrationTask>;
    migrationProcess(callback: Callback<PalletStateTrieMigrationMigrationTask>): Promise<Unsub>;

    /**
     * The limits that are imposed on automatic migrations.
     *
     * If set to None, then no automatic migration happens.
     **/
    autoLimits(): Promise<PalletStateTrieMigrationMigrationLimits | undefined>;
    autoLimits(callback: Callback<PalletStateTrieMigrationMigrationLimits | undefined>): Promise<Unsub>;

    /**
     * The maximum limits that the signed migration could use.
     *
     * If not set, no signed submission is allowed.
     **/
    signedMigrationMaxLimits(): Promise<PalletStateTrieMigrationMigrationLimits | undefined>;
    signedMigrationMaxLimits(callback: Callback<PalletStateTrieMigrationMigrationLimits | undefined>): Promise<Unsub>;
  };
  childBounties: {
    /**
     * Number of total child bounties.
     **/
    childBountyCount(): Promise<number>;
    childBountyCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Number of child bounties per parent bounty.
     * Map of parent bounty index to number of child bounties.
     **/
    parentChildBounties(arg: number): Promise<number>;
    parentChildBounties(arg: number, callback: Callback<number>): Promise<Unsub>;

    /**
     * Child bounties that have been added.
     **/
    childBounties(arg: [number, number]): Promise<PalletChildBountiesChildBounty | undefined>;
    childBounties(
      arg: [number, number],
      callback: Callback<PalletChildBountiesChildBounty | undefined>,
    ): Promise<Unsub>;

    /**
     * The description of each child-bounty.
     **/
    childBountyDescriptions(arg: number): Promise<Bytes | undefined>;
    childBountyDescriptions(arg: number, callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * The cumulative child-bounty curator fee for each parent bounty.
     **/
    childrenCuratorFees(arg: number): Promise<bigint>;
    childrenCuratorFees(arg: number, callback: Callback<bigint>): Promise<Unsub>;
  };
  referenda: {
    /**
     * The next free referendum index, aka the number of referenda started so far.
     **/
    referendumCount(): Promise<number>;
    referendumCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Information concerning any given referendum.
     **/
    referendumInfoFor(arg: number): Promise<PalletReferendaReferendumInfo | undefined>;
    referendumInfoFor(arg: number, callback: Callback<PalletReferendaReferendumInfo | undefined>): Promise<Unsub>;

    /**
     * The sorted list of referenda ready to be decided but not yet being decided, ordered by
     * conviction-weighted approvals.
     *
     * This should be empty if `DecidingCount` is less than `TrackInfo::max_deciding`.
     **/
    trackQueue(arg: number): Promise<Array<[number, bigint]>>;
    trackQueue(arg: number, callback: Callback<Array<[number, bigint]>>): Promise<Unsub>;

    /**
     * The number of referenda being decided currently.
     **/
    decidingCount(arg: number): Promise<number>;
    decidingCount(arg: number, callback: Callback<number>): Promise<Unsub>;

    /**
     * The metadata is a general information concerning the referendum.
     * The `Hash` refers to the preimage of the `Preimages` provider which can be a JSON
     * dump or IPFS hash of a JSON file.
     *
     * Consider a garbage collection for a metadata of finished referendums to `unrequest` (remove)
     * large preimages.
     **/
    metadataOf(arg: number): Promise<H256 | undefined>;
    metadataOf(arg: number, callback: Callback<H256 | undefined>): Promise<Unsub>;
  };
  convictionVoting: {
    /**
     * All voting for a particular voter in a particular voting class. We store the balance for the
     * number of votes that we have recorded.
     **/
    votingFor(arg: [AccountId32Like, number]): Promise<PalletConvictionVotingVoteVoting>;
    votingFor(arg: [AccountId32Like, number], callback: Callback<PalletConvictionVotingVoteVoting>): Promise<Unsub>;

    /**
     * The voting classes which have a non-zero lock requirement and the lock amounts which they
     * require. The actual amount locked on behalf of this pallet should always be the maximum of
     * this list.
     **/
    classLocksFor(arg: AccountId32Like): Promise<Array<[number, bigint]>>;
    classLocksFor(arg: AccountId32Like, callback: Callback<Array<[number, bigint]>>): Promise<Unsub>;
  };
  whitelist: {
    whitelistedCall(arg: H256): Promise<[] | undefined>;
    whitelistedCall(arg: H256, callback: Callback<[] | undefined>): Promise<Unsub>;
  };
  allianceMotion: {
    /**
     * The hashes of the active proposals.
     **/
    proposals(): Promise<Array<H256>>;
    proposals(callback: Callback<Array<H256>>): Promise<Unsub>;

    /**
     * Actual proposal for a given hash, if it's current.
     **/
    proposalOf(arg: H256): Promise<KitchensinkRuntimeRuntimeCall | undefined>;
    proposalOf(arg: H256, callback: Callback<KitchensinkRuntimeRuntimeCall | undefined>): Promise<Unsub>;

    /**
     * Votes on a given proposal, if it is ongoing.
     **/
    voting(arg: H256): Promise<PalletCollectiveVotes | undefined>;
    voting(arg: H256, callback: Callback<PalletCollectiveVotes | undefined>): Promise<Unsub>;

    /**
     * Proposals so far.
     **/
    proposalCount(): Promise<number>;
    proposalCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * The current members of the collective. This is stored sorted (just by value).
     **/
    members(): Promise<Array<AccountId32>>;
    members(callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * The prime member that helps determine the default vote behavior in case of absentations.
     **/
    prime(): Promise<AccountId32 | undefined>;
    prime(callback: Callback<AccountId32 | undefined>): Promise<Unsub>;
  };
  alliance: {
    /**
     * The IPFS CID of the alliance rule.
     * Fellows can propose a new rule with a super-majority.
     **/
    rule(): Promise<PalletAllianceCid | undefined>;
    rule(callback: Callback<PalletAllianceCid | undefined>): Promise<Unsub>;

    /**
     * The current IPFS CIDs of any announcements.
     **/
    announcements(): Promise<Array<PalletAllianceCid>>;
    announcements(callback: Callback<Array<PalletAllianceCid>>): Promise<Unsub>;

    /**
     * Maps members to their candidacy deposit.
     **/
    depositOf(arg: AccountId32Like): Promise<bigint | undefined>;
    depositOf(arg: AccountId32Like, callback: Callback<bigint | undefined>): Promise<Unsub>;

    /**
     * Maps member type to members of each type.
     **/
    members(arg: PalletAllianceMemberRole): Promise<Array<AccountId32>>;
    members(arg: PalletAllianceMemberRole, callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * A set of members who gave a retirement notice. They can retire after the end of retirement
     * period stored as a future block number.
     **/
    retiringMembers(arg: AccountId32Like): Promise<number | undefined>;
    retiringMembers(arg: AccountId32Like, callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The current list of accounts deemed unscrupulous. These accounts non grata cannot submit
     * candidacy.
     **/
    unscrupulousAccounts(): Promise<Array<AccountId32>>;
    unscrupulousAccounts(callback: Callback<Array<AccountId32>>): Promise<Unsub>;

    /**
     * The current list of websites deemed unscrupulous.
     **/
    unscrupulousWebsites(): Promise<Array<Bytes>>;
    unscrupulousWebsites(callback: Callback<Array<Bytes>>): Promise<Unsub>;
  };
  nominationPools: {
    /**
     * The sum of funds across all pools.
     *
     * This might be lower but never higher than the sum of `total_balance` of all [`PoolMembers`]
     * because calling `pool_withdraw_unbonded` might decrease the total stake of the pool's
     * `bonded_account` without adjusting the pallet-internal `UnbondingPool`'s.
     **/
    totalValueLocked(): Promise<bigint>;
    totalValueLocked(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Minimum amount to bond to join a pool.
     **/
    minJoinBond(): Promise<bigint>;
    minJoinBond(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Minimum bond required to create a pool.
     *
     * This is the amount that the depositor must put as their initial stake in the pool, as an
     * indication of "skin in the game".
     *
     * This is the value that will always exist in the staking ledger of the pool bonded account
     * while all other accounts leave.
     **/
    minCreateBond(): Promise<bigint>;
    minCreateBond(callback: Callback<bigint>): Promise<Unsub>;

    /**
     * Maximum number of nomination pools that can exist. If `None`, then an unbounded number of
     * pools can exist.
     **/
    maxPools(): Promise<number | undefined>;
    maxPools(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Maximum number of members that can exist in the system. If `None`, then the count
     * members are not bound on a system wide basis.
     **/
    maxPoolMembers(): Promise<number | undefined>;
    maxPoolMembers(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Maximum number of members that may belong to pool. If `None`, then the count of
     * members is not bound on a per pool basis.
     **/
    maxPoolMembersPerPool(): Promise<number | undefined>;
    maxPoolMembersPerPool(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The maximum commission that can be charged by a pool. Used on commission payouts to bound
     * pool commissions that are > `GlobalMaxCommission`, necessary if a future
     * `GlobalMaxCommission` is lower than some current pool commissions.
     **/
    globalMaxCommission(): Promise<Perbill | undefined>;
    globalMaxCommission(callback: Callback<Perbill | undefined>): Promise<Unsub>;

    /**
     * Active members.
     *
     * TWOX-NOTE: SAFE since `AccountId` is a secure hash.
     **/
    poolMembers(arg: AccountId32Like): Promise<PalletNominationPoolsPoolMember | undefined>;
    poolMembers(arg: AccountId32Like, callback: Callback<PalletNominationPoolsPoolMember | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForPoolMembers(): Promise<number>;
    counterForPoolMembers(callback: Callback<number>): Promise<Unsub>;

    /**
     * Storage for bonded pools.
     **/
    bondedPools(arg: number): Promise<PalletNominationPoolsBondedPoolInner | undefined>;
    bondedPools(arg: number, callback: Callback<PalletNominationPoolsBondedPoolInner | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForBondedPools(): Promise<number>;
    counterForBondedPools(callback: Callback<number>): Promise<Unsub>;

    /**
     * Reward pools. This is where there rewards for each pool accumulate. When a members payout is
     * claimed, the balance comes out fo the reward pool. Keyed by the bonded pools account.
     **/
    rewardPools(arg: number): Promise<PalletNominationPoolsRewardPool | undefined>;
    rewardPools(arg: number, callback: Callback<PalletNominationPoolsRewardPool | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForRewardPools(): Promise<number>;
    counterForRewardPools(callback: Callback<number>): Promise<Unsub>;

    /**
     * Groups of unbonding pools. Each group of unbonding pools belongs to a
     * bonded pool, hence the name sub-pools. Keyed by the bonded pools account.
     **/
    subPoolsStorage(arg: number): Promise<PalletNominationPoolsSubPools | undefined>;
    subPoolsStorage(arg: number, callback: Callback<PalletNominationPoolsSubPools | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForSubPoolsStorage(): Promise<number>;
    counterForSubPoolsStorage(callback: Callback<number>): Promise<Unsub>;

    /**
     * Metadata for the pool.
     **/
    metadata(arg: number): Promise<Bytes>;
    metadata(arg: number, callback: Callback<Bytes>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForMetadata(): Promise<number>;
    counterForMetadata(callback: Callback<number>): Promise<Unsub>;

    /**
     * Ever increasing number of all pools created so far.
     **/
    lastPoolId(): Promise<number>;
    lastPoolId(callback: Callback<number>): Promise<Unsub>;

    /**
     * A reverse lookup from the pool's account id to its id.
     *
     * This is only used for slashing. In all other instances, the pool id is used, and the
     * accounts are deterministically derived from it.
     **/
    reversePoolIdLookup(arg: AccountId32Like): Promise<number | undefined>;
    reversePoolIdLookup(arg: AccountId32Like, callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForReversePoolIdLookup(): Promise<number>;
    counterForReversePoolIdLookup(callback: Callback<number>): Promise<Unsub>;

    /**
     * Map from a pool member account to their opted claim permission.
     **/
    claimPermissions(arg: AccountId32Like): Promise<PalletNominationPoolsClaimPermission>;
    claimPermissions(arg: AccountId32Like, callback: Callback<PalletNominationPoolsClaimPermission>): Promise<Unsub>;
  };
  rankedPolls: {
    /**
     * The next free referendum index, aka the number of referenda started so far.
     **/
    referendumCount(): Promise<number>;
    referendumCount(callback: Callback<number>): Promise<Unsub>;

    /**
     * Information concerning any given referendum.
     **/
    referendumInfoFor(arg: number): Promise<PalletReferendaReferendumInfoTally | undefined>;
    referendumInfoFor(arg: number, callback: Callback<PalletReferendaReferendumInfoTally | undefined>): Promise<Unsub>;

    /**
     * The sorted list of referenda ready to be decided but not yet being decided, ordered by
     * conviction-weighted approvals.
     *
     * This should be empty if `DecidingCount` is less than `TrackInfo::max_deciding`.
     **/
    trackQueue(arg: number): Promise<Array<[number, number]>>;
    trackQueue(arg: number, callback: Callback<Array<[number, number]>>): Promise<Unsub>;

    /**
     * The number of referenda being decided currently.
     **/
    decidingCount(arg: number): Promise<number>;
    decidingCount(arg: number, callback: Callback<number>): Promise<Unsub>;

    /**
     * The metadata is a general information concerning the referendum.
     * The `Hash` refers to the preimage of the `Preimages` provider which can be a JSON
     * dump or IPFS hash of a JSON file.
     *
     * Consider a garbage collection for a metadata of finished referendums to `unrequest` (remove)
     * large preimages.
     **/
    metadataOf(arg: number): Promise<H256 | undefined>;
    metadataOf(arg: number, callback: Callback<H256 | undefined>): Promise<Unsub>;
  };
  rankedCollective: {
    /**
     * The number of members in the collective who have at least the rank according to the index
     * of the vec.
     **/
    memberCount(arg: number): Promise<number>;
    memberCount(arg: number, callback: Callback<number>): Promise<Unsub>;

    /**
     * The current members of the collective.
     **/
    members(arg: AccountId32Like): Promise<PalletRankedCollectiveMemberRecord | undefined>;
    members(arg: AccountId32Like, callback: Callback<PalletRankedCollectiveMemberRecord | undefined>): Promise<Unsub>;

    /**
     * The index of each ranks's member into the group of members who have at least that rank.
     **/
    idToIndex(arg: [number, AccountId32Like]): Promise<number | undefined>;
    idToIndex(arg: [number, AccountId32Like], callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The members in the collective by index. All indices in the range `0..MemberCount` will
     * return `Some`, however a member's index is not guaranteed to remain unchanged over time.
     **/
    indexToId(arg: [number, number]): Promise<AccountId32 | undefined>;
    indexToId(arg: [number, number], callback: Callback<AccountId32 | undefined>): Promise<Unsub>;

    /**
     * Votes on a given proposal, if it is ongoing.
     **/
    voting(arg: [number, AccountId32Like]): Promise<PalletRankedCollectiveVoteRecord | undefined>;
    voting(
      arg: [number, AccountId32Like],
      callback: Callback<PalletRankedCollectiveVoteRecord | undefined>,
    ): Promise<Unsub>;
    votingCleanup(arg: number): Promise<Bytes | undefined>;
    votingCleanup(arg: number, callback: Callback<Bytes | undefined>): Promise<Unsub>;
  };
  assetConversion: {
    /**
     * Map from `PoolAssetId` to `PoolInfo`. This establishes whether a pool has been officially
     * created rather than people sending tokens directly to a pool's public account.
     **/
    pools(
      arg: [PalletAssetConversionNativeOrAssetId, PalletAssetConversionNativeOrAssetId],
    ): Promise<PalletAssetConversionPoolInfo | undefined>;
    pools(
      arg: [PalletAssetConversionNativeOrAssetId, PalletAssetConversionNativeOrAssetId],
      callback: Callback<PalletAssetConversionPoolInfo | undefined>,
    ): Promise<Unsub>;

    /**
     * Stores the `PoolAssetId` that is going to be used for the next lp token.
     * This gets incremented whenever a new lp pool is created.
     **/
    nextPoolAssetId(): Promise<number | undefined>;
    nextPoolAssetId(callback: Callback<number | undefined>): Promise<Unsub>;
  };
  fastUnstake: {
    /**
     * The current "head of the queue" being unstaked.
     *
     * The head in itself can be a batch of up to [`Config::BatchSize`] stakers.
     **/
    head(): Promise<PalletFastUnstakeUnstakeRequest | undefined>;
    head(callback: Callback<PalletFastUnstakeUnstakeRequest | undefined>): Promise<Unsub>;

    /**
     * The map of all accounts wishing to be unstaked.
     *
     * Keeps track of `AccountId` wishing to unstake and it's corresponding deposit.
     **/
    queue(arg: AccountId32Like): Promise<bigint | undefined>;
    queue(arg: AccountId32Like, callback: Callback<bigint | undefined>): Promise<Unsub>;

    /**
     * Counter for the related counted storage map
     **/
    counterForQueue(): Promise<number>;
    counterForQueue(callback: Callback<number>): Promise<Unsub>;

    /**
     * Number of eras to check per block.
     *
     * If set to 0, this pallet does absolutely nothing. Cannot be set to more than
     * [`Config::MaxErasToCheckPerBlock`].
     *
     * Based on the amount of weight available at [`Pallet::on_idle`], up to this many eras are
     * checked. The checking is represented by updating [`UnstakeRequest::checked`], which is
     * stored in [`Head`].
     **/
    erasToCheckPerBlock(): Promise<number>;
    erasToCheckPerBlock(callback: Callback<number>): Promise<Unsub>;
  };
  messageQueue: {
    /**
     * The index of the first and last (non-empty) pages.
     **/
    bookStateFor(arg: number): Promise<PalletMessageQueueBookState>;
    bookStateFor(arg: number, callback: Callback<PalletMessageQueueBookState>): Promise<Unsub>;

    /**
     * The origin at which we should begin servicing.
     **/
    serviceHead(): Promise<number | undefined>;
    serviceHead(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * The map of page indices to pages.
     **/
    pages(arg: [number, number]): Promise<PalletMessageQueuePage | undefined>;
    pages(arg: [number, number], callback: Callback<PalletMessageQueuePage | undefined>): Promise<Unsub>;
  };
  pov: {
    value(): Promise<number | undefined>;
    value(callback: Callback<number | undefined>): Promise<Unsub>;
    value2(): Promise<number | undefined>;
    value2(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * A value without a MEL bound.
     **/
    unboundedValue(): Promise<Bytes | undefined>;
    unboundedValue(callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * A value with a MEL bound of 32 byte.
     **/
    boundedValue(): Promise<Bytes | undefined>;
    boundedValue(callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * 4MiB value.
     **/
    largeValue(): Promise<Bytes | undefined>;
    largeValue(callback: Callback<Bytes | undefined>): Promise<Unsub>;
    largeValue2(): Promise<Bytes | undefined>;
    largeValue2(callback: Callback<Bytes | undefined>): Promise<Unsub>;

    /**
     * A map with a maximum of 1M entries.
     **/
    map1M(arg: number): Promise<number | undefined>;
    map1M(arg: number, callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * A map with a maximum of 16M entries.
     **/
    map16M(arg: number): Promise<number | undefined>;
    map16M(arg: number, callback: Callback<number | undefined>): Promise<Unsub>;
    doubleMap1M(arg: [number, number]): Promise<number | undefined>;
    doubleMap1M(arg: [number, number], callback: Callback<number | undefined>): Promise<Unsub>;
    unboundedMap(arg: number): Promise<Array<number> | undefined>;
    unboundedMap(arg: number, callback: Callback<Array<number> | undefined>): Promise<Unsub>;
    unboundedMap2(arg: number): Promise<Array<number> | undefined>;
    unboundedMap2(arg: number, callback: Callback<Array<number> | undefined>): Promise<Unsub>;
    unboundedMapTwox(arg: number): Promise<Array<number> | undefined>;
    unboundedMapTwox(arg: number, callback: Callback<Array<number> | undefined>): Promise<Unsub>;
  };
  txPause: {
    /**
     * The set of calls that are explicitly paused.
     **/
    pausedCalls(arg: [Bytes, Bytes]): Promise<[] | undefined>;
    pausedCalls(arg: [Bytes, Bytes], callback: Callback<[] | undefined>): Promise<Unsub>;
  };
  safeMode: {
    /**
     * Contains the last block number that the safe-mode will remain entered in.
     *
     * Set to `None` when safe-mode is exited.
     *
     * Safe-mode is automatically exited when the current block number exceeds this value.
     **/
    enteredUntil(): Promise<number | undefined>;
    enteredUntil(callback: Callback<number | undefined>): Promise<Unsub>;

    /**
     * Holds the reserve that was taken from an account at a specific block number.
     *
     * This helps governance to have an overview of outstanding deposits that should be returned or
     * slashed.
     **/
    deposits(arg: [AccountId32Like, number]): Promise<bigint | undefined>;
    deposits(arg: [AccountId32Like, number], callback: Callback<bigint | undefined>): Promise<Unsub>;
  };
  broker: {
    /**
     * The current configuration of this pallet.
     **/
    configuration(): Promise<PalletBrokerConfigRecord | undefined>;
    configuration(callback: Callback<PalletBrokerConfigRecord | undefined>): Promise<Unsub>;

    /**
     * The Polkadot Core reservations (generally tasked with the maintenance of System Chains).
     **/
    reservations(): Promise<Array<Array<PalletBrokerScheduleItem>>>;
    reservations(callback: Callback<Array<Array<PalletBrokerScheduleItem>>>): Promise<Unsub>;

    /**
     * The Polkadot Core legacy leases.
     **/
    leases(): Promise<Array<PalletBrokerLeaseRecordItem>>;
    leases(callback: Callback<Array<PalletBrokerLeaseRecordItem>>): Promise<Unsub>;

    /**
     * The current status of miscellaneous subsystems of this pallet.
     **/
    status(): Promise<PalletBrokerStatusRecord | undefined>;
    status(callback: Callback<PalletBrokerStatusRecord | undefined>): Promise<Unsub>;

    /**
     * The details of the current sale, including its properties and status.
     **/
    saleInfo(): Promise<PalletBrokerSaleInfoRecord | undefined>;
    saleInfo(callback: Callback<PalletBrokerSaleInfoRecord | undefined>): Promise<Unsub>;

    /**
     * Records of allowed renewals.
     **/
    allowedRenewals(arg: PalletBrokerAllowedRenewalId): Promise<PalletBrokerAllowedRenewalRecord | undefined>;
    allowedRenewals(
      arg: PalletBrokerAllowedRenewalId,
      callback: Callback<PalletBrokerAllowedRenewalRecord | undefined>,
    ): Promise<Unsub>;

    /**
     * The current (unassigned) Regions.
     **/
    regions(arg: PalletBrokerRegionId): Promise<PalletBrokerRegionRecord | undefined>;
    regions(arg: PalletBrokerRegionId, callback: Callback<PalletBrokerRegionRecord | undefined>): Promise<Unsub>;

    /**
     * The work we plan on having each core do at a particular time in the future.
     **/
    workplan(arg: [number, number]): Promise<Array<PalletBrokerScheduleItem> | undefined>;
    workplan(arg: [number, number], callback: Callback<Array<PalletBrokerScheduleItem> | undefined>): Promise<Unsub>;

    /**
     * The current workload of each core. This gets updated with workplan as timeslices pass.
     **/
    workload(arg: number): Promise<Array<PalletBrokerScheduleItem>>;
    workload(arg: number, callback: Callback<Array<PalletBrokerScheduleItem>>): Promise<Unsub>;

    /**
     * Record of a single contribution to the Instantaneous Coretime Pool.
     **/
    instaPoolContribution(arg: PalletBrokerRegionId): Promise<PalletBrokerContributionRecord | undefined>;
    instaPoolContribution(
      arg: PalletBrokerRegionId,
      callback: Callback<PalletBrokerContributionRecord | undefined>,
    ): Promise<Unsub>;

    /**
     * Record of Coretime entering or leaving the Instantaneous Coretime Pool.
     **/
    instaPoolIo(arg: number): Promise<PalletBrokerPoolIoRecord>;
    instaPoolIo(arg: number, callback: Callback<PalletBrokerPoolIoRecord>): Promise<Unsub>;

    /**
     * Total InstaPool rewards for each Timeslice and the number of core parts which contributed.
     **/
    instaPoolHistory(arg: number): Promise<PalletBrokerInstaPoolHistoryRecord | undefined>;
    instaPoolHistory(arg: number, callback: Callback<PalletBrokerInstaPoolHistoryRecord | undefined>): Promise<Unsub>;
  };
  tasksExample: {
    /**
     * Some running total.
     **/
    total(): Promise<[number, number]>;
    total(callback: Callback<[number, number]>): Promise<Unsub>;

    /**
     * Numbers to be added into the total.
     **/
    numbers(arg: number): Promise<number | undefined>;
    numbers(arg: number, callback: Callback<number | undefined>): Promise<Unsub>;
  };
  mixnet: {
    /**
     * Index of the current session. This may be offset relative to the session index tracked by
     * eg `pallet_session`; mixnet session indices are independent.
     **/
    currentSessionIndex(): Promise<number>;
    currentSessionIndex(callback: Callback<number>): Promise<Unsub>;

    /**
     * Block in which the current session started.
     **/
    currentSessionStartBlock(): Promise<number>;
    currentSessionStartBlock(callback: Callback<number>): Promise<Unsub>;

    /**
     * Authority list for the next session.
     **/
    nextAuthorityIds(arg: number): Promise<SpMixnetAppPublic | undefined>;
    nextAuthorityIds(arg: number, callback: Callback<SpMixnetAppPublic | undefined>): Promise<Unsub>;

    /**
     * Mixnode sets by session index. Only the mixnode sets for the previous, current, and next
     * sessions are kept; older sets are discarded.
     *
     * The mixnodes in each set are keyed by authority index so we can easily check if an
     * authority has registered a mixnode. The authority indices should only be used during
     * registration; the authority indices for the very first session are made up.
     **/
    mixnodes(arg: [number, number]): Promise<PalletMixnetBoundedMixnode | undefined>;
    mixnodes(arg: [number, number], callback: Callback<PalletMixnetBoundedMixnode | undefined>): Promise<Unsub>;
  };
}
